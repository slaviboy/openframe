# Feature Matrix

The documentation mirror at `../docs-mirror/` is the functional specification.
In-scope products are **Design mode, Draw mode, Dev Mode and Motion mode**. Slides, Make and FigJam are out of scope.
Doc paths below are relative to that mirror.

**Priority**
- **P0**: required for a usable editor
- **P1**: important advanced feature
- **P2**: advanced or edge feature
- **P3**: nice to have

**Status values**
- `Implemented`
- `In progress`
- `Planned`
- `Browser limitation`: works only where the browser supports it; a fallback is noted
- `Not feasible offline`: needs cloud, accounts or AI; the local equivalent is noted

**Milestone** refers to the roadmap in [ARCHITECTURE.md](ARCHITECTURE.md#roadmap).

A row moves to `Implemented` only when the whole chain works:
UI → command → document mutation → history → persistence → renderer → reload restores state.
Its **Tests** column must then name the covering tests.

---

## 1. Editor shell and navigation

| Feature | Documentation | Pri | Dependency | M | Status | Tests |
|---|---|---:|---|---|---|---|
| Navigation bar (main menu, File, Assets, Variables tabs) | explore/explore-the-navigation-bar-and-left-sidebar.html | P0 | UI shell | M1 | Implemented (main menu with Edit/View/Object/Arrange/Page submenus, shortcut labels and full keyboard navigation; File tab; Assets tab (M7); Variables tab opening the variables view over the canvas (M8)) | src/ui/primitives/position.test.ts, e2e/menus.spec.ts, e2e/variables-view.spec.ts |
| Left sidebar: file name menu, Pages, Layers, resizable | explore/explore-the-navigation-bar-and-left-sidebar.html | P0 | Scene graph | M1 | Implemented (file rename, save status, pages, layers, resizable, Find ⌘F, and a menu beside the file name carrying the same actions as the main menu's File section: open and browse files, save, save as and save a local copy, version history and branches, export, and the file thumbnail) | e2e/foundation.spec.ts, e2e/file-menu.spec.ts, e2e/find.spec.ts |
| Right sidebar Design / Prototype tabs (⇧E) | explore/design-prototype-and-explore-layer-properties-in-the-right-sidebar.html | P0 | Inspector | M1 | Implemented (Design tab; Prototype tab M10) | e2e/foundation.spec.ts |
| Floating bottom toolbar with tool groups and dropdowns | explore/access-design-tools-from-the-toolbar.html | P0 | Tools | M1 | Implemented (Move/Hand, Frame, Rectangle/Ellipse groups with dropdown menus) | e2e/foundation.spec.ts |
| Mode switcher Draw / Design / Motion / Dev (⇧D) | explore/access-design-tools-from-the-toolbar.html | P0 | Modes | M11–M13 | Implemented (the toolbar's switcher moves between all four modes, and ⇧D toggles Dev Mode as the reference's does. Draw's toolbar holds the move tools and the illustration tools and the accent turns green; Motion opens the timeline; Dev Mode stands the drawing tools down and reads the design instead. The mode is on the root element as `data-mode`, and the file opens in the mode it was left in, kept per device with the other view preferences. Draw mode is chosen from the switcher, having given up ⇧D to Dev Mode) | e2e/draw-mode.spec.ts |
| Zoom / view options menu (zoom presets, pixel preview, pixel grid, snap to pixel grid, layout guides, rulers, outlines, property labels) | file-utilities/adjust-your-zoom-and-view-options.html | P0 | Viewport | M2 | Implemented (menu on the zoom readout: zoom in/out, to fit, to selection, 50/100/200%; pixel grid ⌘' (drawn at 400%+) and snap to pixel grid ⌘⇧', both on by default; rulers; outlines; property labels (text captions on properties panel number fields). layout guides ⇧G. Pixel preview is a submenu of the same menu — Disabled, 1x or 2x, with a check beside the one in use — and ⌃P (⌃⌥P on Windows) turns it off and back on at the resolution it was last set to; it draws the page onto a surface of its own at one or two pixels to the design pixel, the resolution it would export at, and magnifies that picture to the canvas with no smoothing, so a curve shows the pixels it lands on. Below that zoom there is nothing to magnify and the page is drawn as it always is, and the chrome over it — the pixel grid, the selection, the handles — is never rasterized. The reference's notification confirming the choice is not shown; the check in the menu is what says which is in use) | e2e/view.spec.ts, e2e/guides.spec.ts, e2e/outlines.spec.ts, e2e/pixel-grid.spec.ts |
| Pan (Space-drag, H, trackpad, middle mouse), zoom (⌘-scroll, pinch, ⇧0/⇧1/⇧2, ⌘± around cursor) | explore/explore-design-files.html | P0 | Viewport | M1 | Implemented | src/editor/tools/tools.test.ts, src/editor/commands/commands.test.ts, src/editor/editor.test.ts |
| Minimize UI (⌘⇧\) / Hide UI (⌘\) | file-utilities/hide-or-minimize-the-ui.html | P1 | UI shell | M2 | Implemented (hide shows only the canvas; minimize collapses the sidebars, brings back the properties panel while layers are selected, and offers a Show UI button) | e2e/view.spec.ts |
| Light / dark theme (follows OS, dark fallback) | file-utilities/use-the-actions-menu-in-reference-design.html | P0 | Tokens | M1 | Implemented (System, Light and Dark preference from the main menu or command palette; System follows the OS with a dark fallback; persisted locally) | e2e/view.spec.ts |
| Canvas background color per page | explore/change-the-background-color-of-the-canvas.html | P0 | Page node | M1 | Implemented (editable in the Page section of the properties panel when nothing is selected: the canvas takes the colour, one undo puts it back, and it is kept with the file) | src/engine/render/scene-renderer.test.ts, e2e/page-color.spec.ts |
| Actions menu / command palette (⌘K; also ⌘/) | file-utilities/use-the-actions-menu-in-reference-design.html | P0 | Command registry | M2 | Implemented (command palette: fuzzy search over every command with its shortcut; disabled commands shown but not runnable; ⌘K and ⌘/. Quick insert searches the file's components the same way. The AI actions the reference offers need a model service, so they are not part of an offline editor) | src/editor/commands/fuzzy.test.ts, e2e/menus.spec.ts | src/editor/commands/fuzzy.test.ts, e2e/menus.spec.ts |
| Context menus on canvas, layers and pages (only working actions) | work-with-layers/select-layers-and-objects.html | P0 | Command registry | M2 | Implemented (right-click selects the target and keeps a multi-selection that contains it; object, empty-canvas and page menus built from commands; unavailable actions shown disabled) | src/editor/tools/tools.test.ts, e2e/menus.spec.ts |
| Keyboard-only use (F6 toolbar focus, place object with Return, keyboard box selection) | file-utilities/use-reference-products-with-a-keyboard.html | P1 | Keymap | M2 | Implemented (F6 / Ctrl+F6 focuses the active tool button, ←/→ move between tool buttons and Enter activates one, returning focus to the canvas; with a frame, section, slice or shape tool active, Return places a 100×100 layer centered in the visible canvas; arrow keys pan the canvas with nothing selected; shared keys run the first enabled command. ⌥Space puts out a selection box in the middle of the view, which ⌥ with the arrows moves and ⌥⇧ with them grows, Enter selects what it covers and Escape puts away — and which the command palette can start where a window manager takes the shortcut) | src/editor/keymap/keymap.test.ts, src/editor/tools/place-object.test.ts, e2e/keyboard.spec.ts | src/editor/keymap/keymap.test.ts, src/editor/tools/place-object.test.ts, e2e/keyboard.spec.ts |
| Keyboard shortcuts panel, configurable bindings | file-utilities/use-reference-products-with-a-keyboard.html | P1 | Keymap | M2 | Implemented (⌃⇧? or Main menu → Help: panel docked along the bottom while you keep working, tabs per category listing every registered shortcut, shortcuts already pressed highlighted live and remembered per device. a shortcut is changed from the panel itself: clicking a command's keys listens for the next press and makes that its shortcut, Backspace gives the command its own back, Escape leaves it alone, and Reset all clears every change. The new shortcut works at once and is kept per device; a command with no change of its own keeps what it came with) | src/ui/shortcuts/shortcut-groups.test.ts, e2e/shortcuts.spec.ts |
| Nudge amounts (small/big) preference | file-utilities/set-small-and-big-nudge-values.html | P1 | Preferences | M2 | Implemented (Main menu → Preferences → Nudge amount: small and big distances, validated positive up to 10,000 px, stored per device) | src/editor/commands/nudge.test.ts, e2e/nudge.spec.ts |
| Rulers and guides (drag from ruler, ⌥-drag duplicate, delete, frame-relative) | file-utilities/add-guides-to-the-canvas-or-frames.html | P1 | Viewport, Snapping | M2 | Implemented (rulers ⇧R beside the floating panels with 1/2/5 tick steps and a selection highlight; drag guides from rulers, move, ⌥-drag copy, drop on a ruler or Delete or right-click → Remove guide; page guides snap to whole pixels and layer edges; a new guide dropped over a top-level frame becomes a frame guide; guides persist in the document and undo. Layers snap to page and frame guides while being moved or resized, Control holding them off; with ⌥ held, hovering a guide measures the selection's distance to it.) | src/editor/interactions/guides.test.ts, src/editor/chrome/rulers.test.ts, e2e/guides.spec.ts |
| Outline mode (⌘⇧O), include hidden layers, object bounds | file-utilities/view-layer-outlines-in-reference-design.html | P1 | Renderer | M2 | Implemented (⌘⇧O and zoom menu: hairline outlines of every layer's geometry, black or white by canvas brightness, without fills, strokes or clipping; "Include hidden layers in outlines"; per-device preference. a hidden layer drawn in outline mode takes a click as well, so it can be picked out and worked on; where a stroke sits — inside, outside or astride the edge — is drawn as its own hairline beside the shape, since outline mode paints no strokes; and "Include object bounds" adds the box each layer sits in) | src/engine/render/outline-render.test.ts, e2e/outlines.spec.ts |
| Find and replace (text, layer types, all pages) | file-utilities/find-and-replace-in-reference.html | P1 | Scene graph | M2 | Implemented (Find ⌘F or the search button in the layers header: case-insensitive search on this page or all pages, over the layers' names, the text they carry, or both; type filters for frames, sections, groups, shapes, text and slices; ↑/↓/Enter step through results, clicking selects and brings the layer into view across pages, Esc returns to layers. Reading text also offers Replace all, which rewrites every run of the words in the text that was found — taken as written rather than as a pattern — as one undo step) | src/core/document/find.test.ts, e2e/find.spec.ts | src/core/document/find.test.ts, e2e/find.spec.ts |
| Custom file thumbnail (frame → thumbnail) | file-utilities/set-custom-thumbnails-for-files.html | P2 | Export | M9 | Implemented (Set as thumbnail in a frame's context menu makes it the file's thumbnail, scaled to fill it, and Restore default thumbnail goes back to the first page's content; the frame shows a thumbnail icon in the layers panel; both are undoable; the thumbnail is rendered and stored shortly after the file saves) | src/core/document/file-thumbnail.test.ts, src/platform/idb/persistence.test.ts, e2e/file-browser.spec.ts |
| Local file browser (recents, drafts, trash, import) | import-and-export/import-files-to-the-file-browser.html | P0 | Persistence | M9 | Implemented (File > Files… lists the local files by last edit with their thumbnails, searchable by name; files open (reloading into them), rename, duplicate as "(Copy)" and move to the trash (not the open file), from where they're restored or deleted forever; New file creates and opens an empty file; Import… opens an .openframe file as a new file) | src/core/document/file-thumbnail.test.ts, src/platform/idb/persistence.test.ts, e2e/file-browser.spec.ts |
| AI actions, the reference agent, First Draft, generate/edit images, remove background | explore/use-ai-tools-in-reference-design.html, explore/work-with-the-reference-agent-in-design-files.html | — | Cloud AI | — | Not feasible offline | |
| Plugins, widgets, Tools tab, shaders marketplace | explore/explore-the-navigation-bar-and-left-sidebar.html | — | Cloud | — | Not feasible offline (local equivalent: built-in command palette) | |

## 2. Layers, selection and organization

| Feature | Documentation | Pri | Dependency | M | Status | Tests |
|---|---|---:|---|---|---|---|
| Scene graph (document → pages → nodes; parent/child/sibling) | work-with-layers/parent-child-and-sibling-relationships.html | P0 | Document model | M1 | Implemented | src/core/document/document.test.ts, src/core/scene/scene.test.ts |
| Pages: add, rename, delete, duplicate, reorder, switch | explore/explore-design-files.html | P0 | Scene graph | M1 | Implemented (add, rename (double-click or Enter), switch, and a right-click menu carrying Rename, Duplicate and Delete; pages are dragged into another order, a line showing where one would land, and the move is one undo step) | e2e/foundation.spec.ts, e2e/pages-reorder.spec.ts, src/editor/commands/commands.test.ts, src/editor/commands/pages.test.ts |
| Layers panel: virtualized tree, type icons, expand/collapse, collapse all, bold top-level frames | explore/explore-the-navigation-bar-and-left-sidebar.html | P0 | Scene graph | M1 | Implemented (virtualized tree, type icons, expand/collapse with ⌥ for all descendants, Collapse layers button, bold top-level frames and sections, context menu; layer search through Find ⌘F) | e2e/foundation.spec.ts, e2e/navigation.spec.ts, src/ui/panels/layers/layer-rows.test.ts |
| Rename (double-click, ⌘R batch rename with regex, numbering, preview) | work-with-layers/rename-layers.html | P0 / P2 | Commands | M1 / M2 | Implemented (inline rename by double-click, Enter, F2 or ⌘R with one layer; ⌘R with several layers opens Rename layers: Match as a regular expression, Rename to with Current name `$&`, capture groups, Number ↑ `$n`/`$nnn`, Number ↓ `$N`/`$NNN`, Start from, live preview, invalid expressions reported; one undo step. AI renaming not feasible offline) | e2e/foundation.spec.ts, e2e/rename.spec.ts, src/core/document/batch-rename.test.ts, src/editor/commands/rename.test.ts |
| Lock / unlock (⌘⇧L, drag across rows) | work-with-layers/lock-and-unlock-layers.html | P0 | Commands | M1 | Implemented (⌘⇧L and the row's own toggle, and both lock and hide are dragged down the rows: the press sets the row it began on and every row the pointer travels over takes the same value, all in one undo step) | src/core/scene/scene.test.ts, e2e/layers-lock-drag.spec.ts |
| Hide / show (⌘⇧H, eye icon) | work-with-layers/toggle-visibility-to-hide-layers.html | P0 | Commands | M1 | Implemented | src/editor/commands/commands.test.ts, e2e/foundation.spec.ts |
| Drag to reorder and reparent in the layers panel | explore/explore-the-navigation-bar-and-left-sidebar.html | P0 | Fractional index | M1 | Implemented | src/editor/commands/panel-logic.test.ts, e2e/foundation.spec.ts |
| Click / ⇧-click / ⌘-click deep select, marquee, ⌘-marquee nested | work-with-layers/select-layers-and-objects.html | P0 | Hit testing | M1 | Implemented | src/core/scene/scene.test.ts, src/editor/tools/tools.test.ts, e2e/foundation.spec.ts |
| Hierarchy navigation (Enter child, ⇧Enter parent, Tab / ⇧Tab siblings), select all, inverse, deselect | work-with-layers/select-layers-and-objects.html | P0 | Selection | M2 | Implemented (Enter selects children, ⇧Enter the parent, Tab / ⇧Tab siblings in layers order with wrap-around; select all ⌘A, inverse ⇧⌘A, deselect Esc; navigation keys act only when focus is on the canvas, and unavailable commands leave keys to the browser) | src/editor/commands/commands.test.ts, e2e/navigation.spec.ts |
| Right-click → Select layer (layers under cursor) | work-with-layers/select-layers-and-objects.html | P1 | Hit testing | M2 | Implemented (canvas context menu lists every visible, unlocked layer under the pointer in layers-panel order, including groups whose content is hit and excluding clipped-out content; the current selection is checked) | src/editor/commands/select-similar.test.ts, e2e/select.spec.ts |
| Select matching layers (⌥⌘A), select all with same fill/stroke/effect/font/instance | work-with-layers/identify-matching-objects.html | P1 | Selection | M2 | Implemented (⌥⌘A matches the same chain of layer types and names in sibling top-level frames or groups of the same page or section; select all with the same fill, stroke, effect, font, instance or properties on the current page, from the Edit menu. A property the selection has nothing to say about — a font on a rectangle, an effect on a layer carrying none — matches nothing. Restricting a ⇧-marquee to the matches is left out: the marquee adds to the selection, which is the same reach by hand) | src/editor/commands/select-similar.test.ts | src/editor/commands/select-similar.test.ts, e2e/select.spec.ts |
| Groups (⌘G, ⇧⌘G), frame selection (⌥⌘G) | create-and-edit-layers/the-difference-between-frames-and-groups.html | P0 | Scene graph | M2 | Implemented (groups hug children and are removed when emptied; ungroup also removes frames) | src/editor/commands/structure.test.ts, e2e/structure.spec.ts |
| Frames: presets, clip content, nested frames, resize ignores children with ⌘, hover + to duplicate | create-and-edit-layers/frames-in-reference-design.html | P0 | Scene graph | M1 / M2 | Implemented (while the Frame tool is on, the right sidebar lists frame presets by category — Phone, Tablet, Desktop, Presentation, Watch, Paper and Social media, each with its size — and clicking one places a frame of that size in the middle of the view, named after it; a selected frame's Frame dropdown shows the preset its size matches (or Custom) and resizes frames to another preset; Clip content toggles clipping and frames nest inside frames. An Archive group of superseded devices; ⌘ while resizing leaves the children where they are; and with the Frame tool in hand, hovering a frame shows a + on either side that copies it to that side and pushes the frames beyond it along, in one undo step. The the community preset group is a catalogue of the reference's own asset templates, which isn't available offline) | src/core/document/frame-presets.test.ts, src/editor/commands/frame-presets.test.ts, e2e/frame-presets.spec.ts |
| Auto-parenting when drawing or dropping into frames; Space to prevent | work-with-layers/parent-child-and-sibling-relationships.html | P0 | Hit testing | M1 | Implemented (a shape drawn inside a frame or section becomes its child, and a layer dragged over one is taken into it, keeping where it sits on the canvas; holding Space while drawing or dragging keeps the layer out, so it lands on the page instead. Space only carries this meaning while a gesture is running — with nothing running it still holds the Hand tool.) | src/editor/tools/auto-parenting.test.ts |
| Sections (⇧S, wrap in section, title, fill/stroke, delete keeps content, ready for dev) | work-with-layers/organize-your-canvas-with-sections.html | P1 | Scene graph | M2 | Implemented (Section tool; drawing, moving or resizing a section over layers adopts the ones it fully covers; dragging layers in and out; nested sections; never inside frames or groups (enforced for drawing, dragging, layer drops, paste and file load); title pill selects, drags and double-click renames; fill and stroke; wrap in new section ⌥⌘S; remove section keeping contents ⌘⌫; no rotation or flip. Sections carry a Dev Mode status like frames do, marked ready for dev or completed. Section links are not feasible offline.) | src/editor/commands/sections.test.ts, e2e/sections.spec.ts |
| Slices | import-and-export/export-static-designs-from-reference.html | P1 | Export | M2 | Implemented (Slice tool S; slices are not rendered, show a dashed outline when hovered or selected, can be moved and resized, and only take clicks where no painted layer is hit. A slice exports the region it covers rather than itself, at the scales and formats its export settings say) | src/editor/commands/sections.test.ts, e2e/sections.spec.ts | src/editor/commands/sections.test.ts, e2e/sections.spec.ts |
| Masks (⌃⌘M; alpha / vector / luminance; mask outlines) | create-and-edit-layers/masks.html | P1 | Renderer | M3 | Implemented (Use as mask from the context menu or ⌃⌘M / Ctrl+Alt+M: one layer becomes a mask for the siblings above it; several layers become a "Mask group" whose bottom layer is the mask; the same command removes masks. A mask applies to siblings above it up to the next mask; hidden masks mask nothing. Mask section with Alpha / Vector / Luminance type and Remove mask; mask icon in the layers panel; masked content can only be clicked inside the mask; View › Mask outlines draws masks in green. Hover preview of mask types not implemented) | src/editor/commands/masks.test.ts, src/engine/render/mask-render.test.ts, e2e/masks.spec.ts |
| Multi-edit text and variants (Enter, Q) | work-with-layers/edit-objects-on-the-canvas-in-bulk.html | P2 | Selection | M7 | Implemented (Multi-edit text: with several text layers selected, Multi-edit text at the top of the right sidebar or Return edits them together, so typing replaces the text of every one of them; undo and redo restore each layer's own text, and the result persists. Multi-edit variants: with a component set, a variant or a layer inside a variant selected, Multi-edit variants at the top of the right sidebar or Q marks each variant of the set with a dotted outline; a selected variant selects the other variants and a selected layer the layers with the same names in every variant, so edits reach all of them. Q or Exit multi-edit ends it, as does selecting anything outside the set, such as by clicking or double-clicking elsewhere on the canvas) | src/editor/interactions/text-edit.test.ts, src/editor/commands/multi-edit.test.ts, e2e/multi-edit.spec.ts, e2e/multi-edit-variants.spec.ts |
| Smart selection (spacing handles, swap, reflow on duplicate) | work-with-layers/arrange-layers-with-smart-selection.html | P2 | Snapping | M2 | Implemented (one- and two-dimensional smart selections — two or more layers with equal gaps, in a row or column (1D) or in a grid whose rows hold the same number of layers lined up in columns (2D) — show pink center rings and gap handles, a grid carrying one along each axis and two "space between" fields in the Layout section; dragging a handle or editing a field respaces every gap in one undo step. Clicking a layer's ring marks it (⇧ marks more, double-click marks the whole selection, ⇧ double-click marks that layer's row of a grid), and its ring fills in; the selection's resize handles then act on the marked layers alone and the rest reflow to keep the gaps. Dragging the marked layers carries them to another place — several at once in a row, one at a time in a grid — a blue line showing where they will land, and a drop back where they came from leaves no step in the history; ⌘-dragging a layer onto another of the selection exchanges the two instead, the one under the pointer ringed while the drag is on. ⌘D copies the marked layers into the selection and Delete takes them out of it, the rest moving along either way. The reference's looser 2D arrangements — rows of differing lengths, "lists within lists" — are not read as a smart selection) | src/core/scene/smart-selection.test.ts, src/editor/commands/smart-selection.test.ts, e2e/smart-selection.spec.ts |
| Accessible canvas representation (layer tree mirrors the canvas for screen readers) | view-prototypes/accessible-prototypes-in-reference.html | P1 | Accessibility | M1 | Planned | |

## 3. Transform, arrange, snapping

| Feature | Documentation | Pri | Dependency | M | Status | Tests |
|---|---|---:|---|---|---|---|
| Move / resize handles (⇧ aspect, ⌥ from center), dimension label | work-with-layers/adjust-alignment-rotation-position-and-dimensions.html | P0 | Transforms | M1 | Implemented | src/editor/tools/tools.test.ts, e2e/foundation.spec.ts |
| X/Y/W/H/rotation inputs with math expressions, Mixed, scrubbing labels | work-with-layers/adjust-alignment-rotation-position-and-dimensions.html | P0 | Inspector | M1 | Implemented | src/editor/commands/panel-logic.test.ts, e2e/foundation.spec.ts |
| Rotation (handle, ⇧ 15° steps, ⌥R origin), flip ⇧H / ⇧V | work-with-layers/adjust-alignment-rotation-position-and-dimensions.html | P0 | Transforms | M2 | Implemented (rotation handles outside corners with ⇧ 15° snapping and a live angle label, flip ⇧H/⇧V, rotation field; ⌥R, or the target beside the rotation field, reveals the point the layer turns around on the canvas, which is dragged anywhere on it; the layer turns and scales around that point, in Design and Draw as well as in Motion, where it is also the anchor an animation scales about) | src/editor/tools/tools.test.ts, src/editor/commands/structure.test.ts, src/editor/commands/panel-logic.test.ts, e2e/transform.spec.ts, e2e/structure.spec.ts |
| Aspect ratio lock | work-with-layers/adjust-alignment-rotation-position-and-dimensions.html | P0 | Transforms | M2 | Implemented (Constrain proportions lock between W and H, stored per layer as `constrainProportions`; W/H edits scale the other dimension; handle resizes keep the ratio when every selected layer is constrained, and Shift inverts it; not offered for lines) | src/editor/commands/proportions.test.ts, e2e/proportions.spec.ts |
| Scale tool (K) with multiplier and anchor | work-with-layers/scale-layers-while-maintaining-proportions.html | P1 | Transforms | M2 | Implemented (K or the Move tools menu; handle drags scale layers and their contents proportionally, including stroke weights and corner radii, from the opposite side or the center with ⌥; Scale panel with typed multiplier, presets, proportional W/H and a 3×3 anchor box; locked layers are skipped; effects scale too — shadow offsets, blur radii and spread) | src/editor/commands/scale.test.ts, e2e/scale.spec.ts |
| Nudge with arrows (⇧ for big) | file-utilities/set-small-and-big-nudge-values.html | P0 | Commands | M1 | Implemented (arrow keys nudge by the small amount and ⇧ + arrows by the big amount, one undo step per press, locked layers stay put; arrows act only with focus on the canvas) | src/editor/commands/nudge.test.ts, e2e/structure.spec.ts, e2e/nudge.spec.ts |
| Layer order (⌘] ⌘[ ⌥⌘] ⌥⌘[) | work-with-layers/adjust-alignment-rotation-position-and-dimensions.html | P0 | Commands | M1 | Implemented | src/editor/commands/commands.test.ts |
| Align (⌥A/D/W/S/H/V), ⇧ aligns to parent, distribute, tidy up | work-with-layers/adjust-alignment-rotation-position-and-dimensions.html | P0 | Commands | M2 | Implemented (six align commands, ⇧ aligns each layer to its parent, distribute horizontal/vertical spacing ⌃⌥H/⌃⌥V; tidy up ⌃⌥T arranges the selection into rows by vertical overlap with the median existing gap, top-left anchored, whole pixels, one undo step) | src/editor/commands/align.test.ts, src/core/scene/tidy.test.ts, src/editor/commands/tidy.test.ts, e2e/arrange.spec.ts, e2e/tidy-paste.spec.ts |
| Snapping to geometry, objects, pixel grid; smart guides; spacing and equal-gap indicators; ⌃ disables | work-with-layers/adjust-alignment-rotation-position-and-dimensions.html | P0 | Spatial index | M2 | Implemented (moving, resizing and drawing snap edges and centers to visible siblings and the parent frame within 5 screen px, with red guides; ⌃ disables; ⇧ axis lock respected; positions land on whole pixels while "Snap to pixel grid" is on and on 0.01 px when it is off (rotated layers always 0.01; frames and sections do not yet force pixel snapping when it is off); while moving, a layer between two neighbors in its row or column snaps to equal gaps on axes that didn't snap to an edge, with pink spacing indicators and labels; ⌥-hover shows red distances from the selection to the hovered layer or to its parent frame's edges; a turned selection snaps while it is resized like any other, the corner being dragged snapping wherever it is in the world; and a layer moved past a row or column lands at the spacing that row already uses, matching a gap left elsewhere rather than only centring between two neighbours) | src/core/scene/snapping.test.ts, src/core/scene/equal-gaps.test.ts, src/core/scene/measure.test.ts, src/editor/tools/tools.test.ts, src/editor/tools/measure-tool.test.ts, src/editor/interactions/pixel-snap.test.ts, e2e/arrange.spec.ts, e2e/pixel-grid.spec.ts |
| Measure distances (⌥-hover redlines, ⌘⌥ for nested) | work-with-layers/measure-distances-between-layers.html | P0 | Chrome overlay | M2 | Implemented (with a selection, hold ⌥ and hover: red lines and labels for the gaps to the hovered layer, or for the four distances when one layer contains the other; with nothing else under the pointer, distances go to the selection's parent frame or section. ⌥ hover already reaches nested layers, so ⌘⌥ behaves the same; pressing or releasing ⌥ updates without moving the pointer. Hovering a ruler guide measures the selection's distance to it, to both edges when the guide crosses the selection. A layer that has been turned is measured to the outline it really has rather than the box around it: the line is drawn across the middle of where the two overlap and reaches the edge the shape has at that height, so a corner pointing away reads as further off than its box would.) | src/core/scene/measure.test.ts, src/editor/tools/measure-tool.test.ts |
| Constraints (left, right, left & right, center, scale; ⌘ ignores) | work-with-layers/apply-constraints-to-define-how-layers-resize.html | P0 | Layout | M5 | Implemented (Horizontal constraint (Left, Right, Left & right, Center, Scale) and Vertical constraint (Top, Bottom, Top & bottom, Center, Scale) in the Position section for layers inside frames, left and top by default. Whenever a frame's size changes — resize handles, W/H fields, or a parent's constraints resizing a nested frame — each child keeps its distance to the left/top or right/bottom edge, stretches to keep both, stays centered, or scales proportionally; rotated children move but keep their size. Holding ⌘ while resizing ignores constraints, and the Scale tool scales children instead. Applied at commit and on every preview frame of a drag, from the children's starting values. Not included yet: the dotted constraint lines on the canvas, the interactive constraints diagram, and auto layout frames, whose children are laid out instead (M5)) | src/core/document/constraints.test.ts, e2e/constraints.spec.ts |
| Layout guides (uniform grid, columns, rows; count/auto, margin, gutter, offset; styles) | work-with-layers/create-layout-guides.html | P1 | Renderer, Constraints | M5 | Implemented (Layout guide section for frames: + adds a 10 px uniform grid (red, 10%); each guide has a type (Grid, Columns, Rows), visibility, remove, and settings — grid size; column/row count or Auto, type (Left/Top, Center, Right/Bottom, Stretch), width or height (fixed types, or Auto count), offset (Left/Right/Top/Bottom) or margin (Stretch), gutter, color and opacity. Guides draw over the frame's contents, clipped to it, on unrotated frames; View › Layout guides ⇧G hides all of them. Moving, resizing and drawing snap to columns, rows and grid lines, also while hidden. Constraints of layers in a frame with stretched columns or rows follow the column or row the layer is in. Clicking a guide's row highlights it: ⌘C copies that guide and Paste properties (⌥⌘V or the context menu) adds it to every selected frame. Layout guide styles are M8) | src/core/layout/layout-guides.test.ts, src/editor/clipboard/properties.test.ts, e2e/layout-guides.spec.ts, e2e/layout-guide-copy.spec.ts |
| Copy / cut / paste / paste in place / paste over selection / paste to replace (⇧⌘R) / multi-paste into frames | work-with-layers/copy-and-paste-objects.html | P0 | Clipboard | M2 | Implemented (copy, cut, paste in place ⌥⌘V — which puts the content back at the very coordinates it was copied from, on the page rather than into whatever is selected — paste into selected frame or above selection with position rules, paste over selection ⇧⌘V, paste to replace ⇧⌘R, right-click → Paste here centers the content on the pointer inside the frame or section under it; ⌘V with two or more frames selected pastes one copy into each at the same position relative to the frame it was copied from, centered when copied from outside a frame or when that position falls outside the frame) | src/editor/clipboard/clipboard.test.ts, src/editor/clipboard/paste-here.test.ts, src/editor/clipboard/paste-multi.test.ts, e2e/clipboard.spec.ts, e2e/tidy-paste.spec.ts, e2e/paste-frames.spec.ts |
| Duplicate (⌘D repeats last offset), ⌥-drag duplicate | work-with-layers/copy-and-paste-objects.html | P0 | Commands | M2 | Implemented (⌘D with repeated offset; ⌥-drag duplicate, whose offset a following ⌘D repeats) | src/editor/commands/structure.test.ts, src/editor/tools/tools.test.ts, e2e/structure.spec.ts, e2e/arrange.spec.ts |
| Copy as PNG (⌘⇧C), copy as SVG, copy as code | work-with-layers/copy-and-paste-objects.html | P1 | Export, Codegen | M9 / M13 | Implemented (Copy as PNG (⇧⌘C, or Copy/Paste as in the context menu) copies the selected layer as a PNG at 2× to the system clipboard; Copy as SVG copies its SVG markup as text, noting what the SVG left out; the Copy/Paste as submenu also holds Copy properties and Paste properties; Copy as code copies the layer as the code Dev Mode shows for it, in the language, unit and scale picked there — which are now kept per device, so the panel and the menu always agree) | e2e/copy-as.spec.ts, e2e/properties.spec.ts |
| Copy / paste properties (⌥⌘C / ⌥⌘V, single fill/stroke/effect rows) | work-with-layers/copy-and-paste-properties-between-layers.html | P1 | Clipboard | M3 | Implemented (Copy properties / Paste properties in the context menu, ⌥⌘C / ⌥⌘V (Ctrl+Alt+C / V): opacity, blend mode, effects, fills, strokes and stroke settings, corner radius and independent corners, per-side stroke weights — each applied only where the target layer type supports it, with absent optional properties cleared, one undo step for all selected layers. Clicking a fill, stroke or effect row highlights it; ⌘C then copies only that row and ⌘V adds it on top of each selected layer's list. Properties travel as validated `data-openframe-properties` HTML on the system clipboard when the browser allows, with an in-tab fallback. Text properties arrive with the text engine (M4)) | src/editor/clipboard/properties.test.ts, e2e/properties.spec.ts |
| Copy between browser tabs/windows | work-with-layers/copy-and-paste-objects.html | P1 | Clipboard (native copy/paste events with an HTML flavor carrying a validated payload) | M2 | Implemented (implemented through the system clipboard; the cross-tab path cannot be automated in Playwright, so it has a manual test step in TESTING.md) | src/editor/clipboard/clipboard.test.ts |

## 4. Shape, drawing and vector tools

| Feature | Documentation | Pri | Dependency | M | Status | Tests |
|---|---|---:|---|---|---|---|
| Rectangle (R), Ellipse (O), Frame (F / A) | create-and-edit-layers/shape-tools.html | P0 | Tools | M1 | Implemented | src/editor/tools/tools.test.ts, e2e/foundation.spec.ts |
| Line (L), Arrow (⇧L), Polygon (point count), Star (count, ratio, radius) | create-and-edit-layers/shape-tools.html | P0 | Tools | M2 | Implemented (Line/Arrow tools with ⇧ 45° constraint, snapping and click-to-place; end point handles move one end; start/end point markers: none, line arrow, triangle arrow, round, square, circle, diamond. Polygon and Star tools with Count 3–60 and star Ratio; hit testing follows outlines. Polygons and stars take a corner radius) | src/editor/tools/line-tool.test.ts, src/engine/render/shapes-render.test.ts, src/core/geometry/shapes.test.ts, e2e/shapes.spec.ts |
| Arc handles on ellipse (sweep, start, ratio ring) | create-and-edit-layers/arc-tool-create-arcs-semi-circles-and-rings.html | P1 | Geometry | M6 | Implemented (Hovering a selected ellipse shows the sweep handle just inside its right-hand side; dragging it up or down cuts a gap, with the sweep shown as a positive or negative percentage. The start handle (with a dot) then turns the arc and the ratio handle, at the center, hollows it into a ring; dragging the sweep back to the start closes a ring. Arcs, pies and rings render, are clickable only where filled, flatten and outline with their shape, and persist; each drag is one undo step. Dragging the ratio handle around into the gap shows the other segment instead, and back again) | src/core/geometry/arc.test.ts, src/editor/interactions/arc-handles.test.ts, e2e/arc.spec.ts |
| Pencil (⇧P), smoothing, ⇧ straight lines | create-and-edit-layers/sketch-on-the-canvas-with-the-pencil-tool.html | P0 | Vector model | M6 | Implemented (Pencil ⇧P, in the toolbar: dragging sketches a vector path — the pointer's path simplified within a screen pixel and smoothed into curves — with a round 3 px black stroke; holding Shift draws a straight line from the start to the pointer; the tool stays active for the next sketch. A sketch still drawn in the black it starts out with turns white where the canvas or the frame under the pointer is dark, so the line can be seen; a stroke the designer picked is drawn as it is.) | src/core/vector/pencil.test.ts, src/editor/tools/pen-tool.test.ts, e2e/pen-pencil.spec.ts |
| Pen (P): vector networks, curves, close/open paths | design-with-vector-tools/vector-networks.html | P0 | Vector model | M6 | Implemented (Pen P, in the toolbar: each click adds a point to a new vector layer; dragging while placing a point pulls out symmetric Bézier handles; clicking a point of the path closes it into a region that fills can paint; Escape or another tool finishes an open path, and a lone point is discarded. Vector layers keep the reference's vector network model — vertices, straight or curved segments in any direction, closed regions with a winding rule — render fills in regions and strokes along every segment with an endpoint cap, can be selected by their regions or strokes, and scale their network when resized. Points snap to the edges, centres and guides around them while the path is drawn, Control placing one exactly where the pointer is; a thin dashed line trails the pointer from the point last placed; and pressing on the open end of a path already drawn carries that path on rather than beginning another layer (for layers sitting square in their parent, since the Pen draws in the parent's space). Vector edit mode is its own row.) | src/core/vector/vector-network.test.ts, src/core/vector/pen.test.ts, src/core/vector/vector-finalizer.test.ts, src/editor/tools/pen-tool.test.ts, e2e/pen-pencil.spec.ts |
| Vector edit mode with secondary toolbar | design-with-vector-tools/edit-vector-layers.html | P0 | Vector model | M6 | In progress (Return or double-clicking a vector layer enters vector edit mode, drawn as its segments and square points: click a point to select it (Shift adds or removes), drag selected points to move them — curves keep their handles and the layer box refits — double-click a path to add a point without changing its shape, Delete removes the selected points with their segments, and clicking inside the layer clears the point selection. Return, Escape or clicking elsewhere leaves. While editing, the toolbar becomes the secondary toolbar with Move (V), Lasso (Q) — a drag draws an outline that selects the points inside it, Shift adds — Cut (X), Bend, Paint (⇧B), Eraser (⇧E), Variable width and Done; selected points show their Bézier handles, which the Move tool drags (handles that mirrored each other keep mirroring). With two or more points selected, the Move tool shows their bounding box: dragging an edge or corner resizes them (Shift keeps proportions, Alt resizes from the center) and dragging from just outside a corner rotates them (Shift snaps to 15°). Holding Space while dragging the points' box carries them about instead of resizing or turning them, and letting go picks up where it left off. A shape drawn some other way — a rectangle, an ellipse, a polygon, a star, a line — becomes a vector layer of the same outline in its place when its points are opened, keeping what every layer carries; layers that hold other layers are left alone. Editing several layers at once is pending, vector edit mode taking one layer at a time) | src/core/vector/vector-edit.test.ts, src/editor/interactions/vector-edit.test.ts, src/editor/interactions/vector-lasso.test.ts, e2e/vector-edit.spec.ts, e2e/vector-lasso.spec.ts, src/core/vector/vector-transform-points.test.ts, src/editor/interactions/vector-points-box.test.ts, e2e/vector-points-box.spec.ts |
| Bend tool with mirroring modes | design-with-vector-tools/edit-vector-layers.html | P0 | Vector model | M6 | Implemented (Bend in vector edit mode's secondary toolbar: pressing on a point, or anywhere on a path (adding a point there), and dragging pulls out mirrored Bézier handles — the side leaving the point follows the pointer — so the path curves smoothly through it; one undo step. Selected points show their handles, and the Move tool drags them, keeping handles that mirrored each other mirrored. Shift-clicking handles selects them, and dragging one moves every selected handle the same way. A point carries its own Mirroring — no mirroring, mirror angle, or mirror angle and length — set from the Mirroring section of the properties panel while points are selected and kept on the point in the file; a point with none reads its mirroring from its handles, so one the Bend tool made mirrors angle and length and a corner mirrors nothing.) | src/core/vector/vector-bend.test.ts, src/editor/interactions/vector-bend.test.ts, e2e/vector-bend.spec.ts |
| Cut tool (X), Lasso (Q), Paint bucket (⇧B), Eraser (⇧E) | design-with-vector-tools/edit-vector-layers.html | P1 | Planarization | M6 | Implemented (In vector edit mode's secondary toolbar: Lasso (Q) — dragging draws an outline that selects the points inside it, Shift adds, and a click clears the point selection — and Cut (X) — clicking a point or anywhere on a path breaks the path there into two ends, which stay selected, and a region whose outline is broken loses its fill; dragging across paths cuts them along the line, the piece with the first point staying in the layer and each other piece moving to a layer of its own above it with the same name and appearance; a closed region the drag crosses divides into the parts of its area on each side of the line, each keeping the region's fills; one undo step — and Paint (⇧B) — clicking a closed region fills it with the paint picked in the properties panel's Paint section (a solid color; by default the layer's first solid fill), clicking a region that already shows that paint removes its fill, and a drag paints every region it crosses, as one undo step; the region under the pointer is striped, blue to fill and red to remove — and the Eraser (⇧E) — dragging removes the area a round eraser (its weight set in the properties panel's Eraser section) passes over: open paths are clipped where they cross it, and closed regions lose that area, the edges of what remains becoming new points and segments that keep the region's own fills; one undo step. The Paint tool paints with a gradient as readily as a solid colour — its Paint section picks the type, and a paint not set for the tool follows the layer's first visible fill, whose stops the Fill section shapes — and carries a droplet cursor, filled where a click would paint a region and hollow where it would take a region's fill away. The Eraser rubs out with a round or a square shape, picked in its section.) | src/editor/interactions/vector-lasso.test.ts, src/core/vector/vector-cut.test.ts, src/editor/interactions/vector-cut.test.ts, src/core/vector/vector-paint.test.ts, src/editor/interactions/vector-paint.test.ts, e2e/vector-lasso.spec.ts, e2e/vector-cut.spec.ts, e2e/vector-paint.spec.ts, src/core/vector/vector-erase.test.ts, src/editor/interactions/vector-eraser.test.ts, e2e/vector-eraser.spec.ts, src/core/vector/vector-divide.test.ts, src/editor/interactions/vector-divide.test.ts, e2e/vector-divide.spec.ts, e2e/vector-divide-fill.spec.ts |
| Variable-width strokes (width points, profiles) | design-with-vector-tools/edit-vector-layers.html | P1 | Stroke geometry | M6 | Implemented (Variable width in vector edit mode's secondary toolbar, on vector layers whose path doesn't branch and isn't dashed: hovering the stroke shows where a width point would go; clicking adds one at the current width, snapped to a vector point, halfway between two vector points or between two width points (Ctrl skips snapping); dragging a knob at either side sets the width, dragging the point moves it along the path, Shift-click selects several, Delete removes them, and the properties panel's Width point section edits their width. The stroke is drawn as an outline whose width eases from point to point, and persists. A Width profile section offers the shapes a width can take along the stroke — uniform, tapered to the end, from the start or at both ends, wide in the middle, narrow in the middle — laid down as a share of the stroke's own weight so a profile keeps its look at any weight, and read back from the points so the one in use is shown. Outline stroke follows the shape a varying stroke is drawn as rather than stroking its centreline, and clicking the stroke hits it where it is actually drawn, not within a width it only reaches in places.) | src/core/vector/vector-width.test.ts, src/editor/interactions/vector-width.test.ts, e2e/vector-width.spec.ts |
| Shape builder (merge, extract, ⌥ subtract) | design-with-vector-tools/create-custom-shapes-with-the-shape-builder-tool.html | P1 | Planarization | M6 | Implemented (Shape builder in vector edit mode's secondary toolbar, working on the layer being edited: its closed regions are cut into the separate pieces they overlap into — each piece the area inside one group of the regions and outside the rest, worked out with Skia path ops, up to eight regions since every group of them is tried — and the piece under the pointer is washed over so it can be seen. Clicking a piece takes it out onto a layer of its own directly above, keeping the layer's appearance; ⌥-clicking takes it away; dragging across several gathers them and merges them into one shape. Each is one undo step, and all of it is destructive, as the documentation says. Working on several layers at once is pending, vector edit mode taking one layer at a time.) | src/core/vector/shape-builder.test.ts, e2e/shape-builder.spec.ts |
| Boolean operations: union, subtract, intersect, exclude (⌥⇧U/S/I/E), live boolean groups | design-with-vector-tools/boolean-operations.html | P0 | Skia PathOps | M6 | In progress (⌥⇧U/S/I/E, Object menu and layer context menu wrap two or more shapes, vectors, text layers or groups (not frames, sections or slices) in a boolean group named after the operation; it takes its fill, stroke and effects from the top layer (the bottom layer for subtract). The group draws its children's outlines combined with Skia path ops — union, subtract (the bottom layer minus the ones above), intersect, exclude — painted with its own appearance; the children stay selectable and editable, the group hugs them and disappears when emptied, clicks hit only the combined shape, and Ungroup (⇧⌘G) releases the layers. ⌥⇧S aligns bottom to the parent when a boolean operation doesn't apply. The toolbar carries a Boolean operations menu with the four operations and Flatten, offered while the selection is something they apply to; running one on a boolean group already selected changes that group's operation rather than wrapping it again, renaming it when it was still called after the old one. A child's stroke goes into the combined shape, so a stroked layer contributes the ink it draws. A boolean group flattens into the shape it combines to. Text inside a boolean group still contributes its box rather than its glyphs: the shape is built while a frame is drawn, and glyph outlines are read from a font file that has to be waited for, so they would have to be worked out and kept ahead of time) | src/core/scene/boolean-hit.test.ts, src/editor/commands/boolean.test.ts, e2e/boolean.spec.ts |
| Flatten (⌥⇧F) | design-with-vector-tools/flatten-layers.html | P0 | Geometry | M6 | Implemented (⌥⇧F, Object menu and layer context menu: the selected layers merge into one vector layer in the topmost layer's place — every shape (rectangles and frames with their corners, ellipses, polygons, stars, lines, vectors) keeps its outline, transformed into the shared parent's space, and closed outlines stay fillable regions; containers contribute their contents and are removed; the vector takes the topmost layer's name and appearance; one undo step. Text flattens too: the text layers at or under the selection are read out as their glyphs' outlines first, in the same undo step, so a group of words and shapes merges into one layer; text whose outlines can't be read is left as it is and contributes nothing (glyph outlines)) | src/core/vector/shape-networks.test.ts, src/core/vector/flatten.test.ts, e2e/flatten.spec.ts |
| Outline stroke (⌘⌥O) | design-with-vector-tools/convert-strokes-to-vector-paths.html | P1 | Geometry | M6 | Implemented (⌘⌥O, Object menu and layer context menu: each selected rectangle, ellipse, polygon, star, line or vector layer with a visible stroke becomes a vector layer of the area its stroke covers — dashes, caps, joins and inside/outside alignment applied with Skia path ops — filled with the stroke's paints. A layer that also has a visible fill keeps its fill and loses its stroke, with the outlined stroke placed directly above it; one undo step, and the outlines are selected. A text layer's stroke is outlined through its glyphs: they are read out first and that outline is stroked, the text going in favour of the two vector layers. Frames outline the stroke around their box and keep their contents; a boolean group outlines the stroke around the shape it combines to; per-side stroke weights outline as the ring they are drawn as; and a dash pattern of any length is cut by hand, since Skia's own dashing takes only one length on and one off. A line's end markers — arrows, triangles, circles and diamonds — go into the outline with its stroke, so an arrow outlines with its head) | src/editor/commands/outline-stroke.test.ts, e2e/outline-stroke.spec.ts |
| Convert text to vector paths | design-with-vector-tools/convert-text-to-vector-paths.html | P1 | Text, Geometry | M6 | Implemented (Convert text to vector paths in the Object menu and the layer context menu: each selected text layer becomes a vector layer of its glyphs' outlines in its place, keeping its fills, opacity, blend mode and effects; one undo step. The glyphs are placed from the engine's own layout — so line breaking, alignment and letter spacing are the ones that were on the canvas — and their outlines are read with opentype.js, a character the layer's font can't draw coming from the first bundled font that has it. The bundled fonts ship as WOFF2, whose tables are packed; every browser WOFF2 decoder builds its bindings with `new Function`, which the app's content security policy forbids, so they are unpacked at build time by `scripts/vite-plugin-font-outlines.ts` and fetched a font at a time, like the CJK subsets. A user font uploaded as WOFF2 therefore has no outlines to read, and complex scripts whose glyphs are joined or reordered by shaping come out per character) | src/editor/commands/outline-text.test.ts, e2e/outline-text.spec.ts |
| Offset path (amount, join) | design-with-vector-tools/offset-a-vector-path.html | P2 | Geometry | M6 | Implemented (Offset vector in the Object menu and the layer context menu, for vector layers that enclose an area: a dialog with Amount — positive grows the shape, negative shrinks it — and a Round or Square join, shown on the canvas as it is typed and taken by Enter or Apply, in one undo step; Escape leaves the layer as it was. The growth is a stroke of twice the amount laid along the outline and taken into the area (cut away from it, when shrinking), so the corners turn by the join; shrinking a shape away leaves it as it was rather than emptying it. Offsetting an open path, which has no area to grow, is not offered) | src/editor/commands/offset-path.test.ts, e2e/offset-path.spec.ts |
| Simplify path (slider; ⇧Delete deletes and heals) | design-with-vector-tools/simplify-a-vector-path.html | P2 | Curve fitting | M6 | Implemented (⇧Delete in vector edit mode deletes the selected points and heals the path: where a point joins exactly two segments they become one segment between their far ends, keeping the outer handle directions (lengthened to the joined length) and the region fill; endpoints and junction points are deleted with their segments; one undo step. Simplify vector in the Object menu and the layer context menu opens a slider: each contour of the path is flattened, thinned of the points that say least about its shape, and drawn again as a smooth curve, while the turns it goes sharply around stay sharp, so a square stays square; the canvas shows it and the point count as the slider moves, Enter or Simplify keeps it and Escape leaves the path alone; one undo step. A branching path — one where a point joins more than two segments — is left as it is. Selecting points with the Lasso was already there.) | src/core/vector/vector-heal.test.ts, src/core/vector/simplify.test.ts, src/editor/commands/simplify-path.test.ts, e2e/vector-heal.spec.ts, e2e/simplify-path.spec.ts |
| Corner radius and smoothing, independent corners, iOS preset, on-canvas radius handles | additional-properties/adjust-corner-radius-and-smoothing.html | P0 | Geometry | M1 / M3 | Implemented (uniform radius for frames, rectangles, polygons and stars — polygon/star vertices round with tangent arcs clamped to half of each adjacent edge; Independent corners toggle with four per-corner fields for frames and rectangles, returning to the largest corner when turned off; radii scale with the Scale tool. Corner smoothing 0–100% with an iOS (60%) preset renders squircle corners (the arc of a round corner shortened and eased in with Béziers), clips frame content, hit-tests the smoothed outline and copies with Copy properties. On-canvas radius handles appear when hovering a selected rectangle, polygon or star: drag along the corner's bisector, ⌥ for a single rectangle corner, live radius label. Per-point radius in vector edit mode arrives with vector networks (M6), and variables on corner fields with M7) | src/core/geometry/corners.test.ts, src/editor/interactions/radius-handles.test.ts, src/editor/commands/corner-radius.test.ts, src/engine/render/shapes-render.test.ts, src/core/geometry/shapes.test.ts, e2e/corner-radius.spec.ts, e2e/corner-handles.spec.ts |
| Image / video placement tool (⇧⌘K, place multiple) | color-gradients-and-images/add-images-and-videos-to-designs.html | P0 | Assets | M3 | Implemented (Place image in the Shape tools menu or ⇧⌘K opens the file picker for PNG, JPEG, WebP, GIF and other browser-decodable images; a hint shows the next image and how many remain; each click places one — on a rectangle, ellipse, polygon or star it replaces the top fill, elsewhere it creates a layer at the image's pixel size inside the frame under the click; Esc, Delete or switching tools discards the rest; with several waiting, Place all in the hint places them all in a row at the center of the view and selects them. Dropping image files on the canvas and pasting images from the clipboard place them at the drop point or the center of the view, several in a row. Images over 4096px are scaled down. SVG files import as editable vectors instead (see Import SVG); video files place the same way, as video fills (see Video / GIF fills)) | src/editor/commands/images.test.ts, e2e/images.spec.ts, e2e/video-fills.spec.ts |

## 5. Text and typography

| Feature | Documentation | Pri | Dependency | M | Status | Tests |
|---|---|---:|---|---|---|---|
| Text tool (click → auto width, drag → fixed size), editing, caret, selection, IME | text-and-typography/guide-to-text-in-reference-design.html | P0 | SkParagraph | M4 | Implemented (Text tool T in the toolbar: a click creates an auto-width layer with its first line centered on the click, a drag creates a fixed-size box; editing starts at once and creating plus typing is one undo step, and a layer left empty is removed without a trace. Return or double-click edits a text layer (Return selects all, double-click places the caret). Typing, IME composition, line breaks, Backspace/Delete by character, word (⌥) or line (⌘), arrows with Shift to extend, ⌥ word and ⌘ line/text jumps, Home/End, ⌘A, copy/cut/paste of plain text, and undo/redo within the session through a hidden textarea; click places the caret, Shift-click extends, double-click selects a word, triple-click a paragraph, dragging selects; clicking another text layer edits it, clicking elsewhere or Esc ends editing. The selection highlight and a blinking caret draw on the overlay. Layer names follow the first line until renamed. Multi-edit text: with several text layers selected, Multi-edit text at the top of the right sidebar or Return edits them together — they stay selected and every change to the text applies to all of them (layers whose text matched keep their styles), undo while editing restores each layer's own text, the session is one undo step, and layers left empty are removed. Spell check (Preferences › Check spelling, on by default): while text is edited, words the dictionary doesn't know are underlined with a red wavy line, and right-clicking one lists up to five suggestions that replace it; numbers, words with digits and all-caps abbreviations aren't checked. The English Hunspell dictionary is bundled and loads the first time text is edited; other languages aren't available offline. Text on a path came with M11) | src/core/text/text-editing.test.ts, src/editor/interactions/text-edit.test.ts, src/core/history/history-merge.test.ts, e2e/text.spec.ts |
| Resizing: auto width, auto height, fixed, truncate, max lines | text-and-typography/adjust-text-dimensions-and-resizing.html | P0 | Layout | M4 | Implemented (`textAutoResize` WIDTH_AND_HEIGHT, HEIGHT, NONE and TRUNCATE are laid out and rendered: auto width fits both dimensions and grows from the edge its alignment anchors, auto height wraps to the width and fits the height, fixed boxes wrap and overflow with vertical alignment, truncate ends in an ellipsis on the last line that fits; a history finalizer keeps boxes fitted at every commit. Resizing buttons (Auto width, Auto height, Fixed size, Truncate text) in the Layout section; typing a width wraps auto-width text into auto height and typing a height fixes the box, and resize handles do the same (side handles wrap, handles that change the height fix). Max lines in Type settings cuts auto height and truncated text off with an ellipsis after that many lines, and auto height fits the limited text) | src/core/text/text-resize.test.ts, src/engine/text/text-shaper.test.ts, src/editor/commands/text.test.ts, e2e/typography.spec.ts |
| Font family, weight/style, size, line height, letter spacing, horizontal and vertical alignment | text-and-typography/explore-text-properties.html | P0 | Text engine | M4 | Implemented (Typography section for one or more text layers: font family (fonts missing from this device are listed as missing), font style with all 18 weights upright and italic, font size, line height (Auto, a number in pixels, or a percentage), letter spacing (percent or pixels; a bare number keeps the unit), horizontal alignment (left, center, right, justify) and vertical alignment (top, middle, bottom), each one undo step and mixed-aware. Shaped by SkParagraph with the bundled Inter variable font — style names map to the weight axis and italic — and Latin-extended, Cyrillic, Greek and Vietnamese subsets as fallbacks; fills of every paint type color the glyphs; outline mode draws glyph outlines; the Scale tool scales font size and pixel spacing. Per-character styles are the mixed styles row) | src/core/text/font-style.test.ts, src/core/text/text-values.test.ts, src/engine/text/text-shaper.test.ts, src/editor/commands/text.test.ts, e2e/typography.spec.ts |
| Mixed styles within one text layer | text-and-typography/explore-text-properties.html | P0 | Rich runs | M4 | Implemented (while editing with characters selected, the Typography section (font family, style, size, line height, letter spacing) and the Fill section show and change just those characters; ⌘B / ⌘I toggle bold and italic on the selection. Without a selection, the same controls and ⌘B / ⌘I apply to whole layers and clear that property's per-character overrides; layers with differing characters show Mixed. Styles live in `styleRuns` (sorted, merged, normalized against the layer style), follow every edit (typed text takes the style before the caret), survive in-session undo, scale with the Scale tool, and render as SkParagraph style runs with per-range fill layers of any paint type) | src/core/text/style-runs.test.ts, src/editor/commands/text.test.ts, e2e/mixed-styles.spec.ts |
| Type settings: decoration (underline options), letter case, vertical trim, paragraph spacing, indentation, hanging punctuation, wrap style | text-and-typography/explore-text-properties.html | P1 | Text engine | M4 | Implemented (Type settings in the Typography section: underline and strikethrough buttons, letter case (as typed, uppercase, lowercase, capitalize, small caps) and max lines, for whole layers or the selected characters. Decorations take each range's fill color; letter case is applied when shaping without changing the stored characters, keeping every offset; small caps uses the font's `smcp` feature. Paragraph spacing and paragraph indent (the indent only applies to left-aligned and justified text, and its field is disabled otherwise): each paragraph is laid out on its own and stacked, with max lines and truncation shared across paragraphs and carets, hit testing, selection and line navigation crossing paragraph boundaries. Wrap style per paragraph (or whole layers): Auto; Balance, which lays the paragraph out at the narrowest width that keeps its number of lines, so lines come out even; and Pretty, which narrows the paragraph just enough (at most 20%) to bring a second word down to an orphaned last line without adding a line — an approximation of the reference, which adjusts only the last lines; centered and right-aligned wrapped text stays aligned in the box, and auto-width text never wraps. Hanging lists put first-level markers outside the text box so item text aligns with its edge. Underline details for underlined text, per character: style (solid, dotted, wavy), thickness (Auto — the font's own underline thickness — or pixels), offset from the font's underline position, skip ink (on by default: the underline breaks where descenders cross it) and color (the text's color or a custom color with opacity); underlines are drawn by the editor from the font's underline metrics, once per layer. Hanging quotes: an opening quotation mark (straight, curly, guillemet, low-9 or corner bracket) starting a left-aligned or justified, left-to-right paragraph that isn't a list item sits outside the text box on the first line, so the text aligns with the box edge; the box keeps its size, and carets, clicks, selection, line navigation and underlines follow the shifted line. Vertical trim (Type settings): in auto layout the text takes up only the space from its first line's cap height to its last baseline, while the text layer keeps its own size and position within that space) | src/core/text/letter-case.test.ts, src/core/text/paragraphs.test.ts, src/editor/commands/text.test.ts, src/engine/text/text-shaper.test.ts, e2e/type-settings.spec.ts, e2e/paragraphs.spec.ts, src/core/text/wrap-style.test.ts, e2e/wrap-style.spec.ts, src/core/layout/auto-layout.test.ts, e2e/vertical-trim.spec.ts |
| Bulleted and numbered lists, nesting | text-and-typography/create-bulleted-and-numbered-lists.html | P1 | Text engine | M4 | Implemented (lists belong to paragraphs: ⌘⇧8 (also ⌥8 on macOS) toggles a bulleted list and ⌘⇧7 a numbered list for the paragraphs under the caret or selection, or for whole selected layers; typing "- ", "* ", "1. " or "1) " at the start of a paragraph starts one. Return continues the list, Return on an empty item moves it out a level and then ends the list, Backspace at the start of an item removes its marker, and Tab / ⇧Tab or ⌘] / ⌘[ change the indentation across 5 levels. Every line of an item is indented by its level with the marker hanging in front; numbers count per level (1. / a. / i. by level), resume after deeper items and restart after text that isn't a list. Type settings has List style, Decrease/Increase indentation and List spacing (space between consecutive items)) | src/core/text/lists.test.ts, src/editor/commands/text.test.ts, src/editor/interactions/text-edit.test.ts, src/engine/text/text-shaper.test.ts, e2e/lists.spec.ts |
| Links in text | text-and-typography/add-links-to-text.html | P2 | Rich runs | M4 | Implemented for web addresses (while editing, ⇧⌘U or Create link in Typography opens an address field above the selected characters — for a selected layer, all of its text — and Return links and underlines them; pasting an http(s) or www. address over selected characters links them, ⇧⌘V pastes it as text. With the caret in a link a bar above it shows the address with Open (a new browser tab), Edit and Remove (which also removes the underline). Addresses are normalized to http or https; other schemes such as javascript: and mailto: are refused, as in the reference. Typing right after a link doesn't extend it. Links to the reference files, pages and frames need the reference's sharing URLs and are not offered; prototype link following arrives with M10) | src/core/text/links.test.ts, src/editor/commands/text.test.ts, src/editor/interactions/text-edit.test.ts, e2e/links.spec.ts |
| OpenType features (stylistic sets, numbers, ligatures) | text-and-typography/use-opentype-features.html | P1 | SkParagraph fontFeatures | M4 | Implemented (Type settings, for whole layers or the selected characters: Numbers — figure spacing (proportional or monospace/tabular), figure style (uppercase/lining or lowercase/old-style), position (superscript or subscript), fractions and slashed zero; case-sensitive forms and capital spacing when the font has them; and an OpenType features list grouped as Letterforms (ligatures, discretionary ligatures, contextual alternates, stylistic alternates, ordinals, small caps from capitals), Stylistic sets 1–20, Character variants 1–30, Horizontal spacing (kerning) and Numbers (numerators, denominators, scientific inferiors), with the defaults (ligatures, contextual alternates, kerning) shown on. Support is detected per font and style by shaping sample text with each feature switched; unsupported settings are disabled or not listed. Settings are stored per character and only differences from the font default are kept. The bundled Inter subset supports tabular figures, fractions, numerators/denominators, contextual alternates and kerning; fonts you add expose theirs. Not included: the hover preview panel, font-defined names for stylistic sets, and synthesized ("faux") superscript/subscript for fonts without those glyphs) | src/core/text/opentype.test.ts, src/engine/text/text-shaper.test.ts, src/editor/commands/text.test.ts, e2e/opentype.spec.ts |
| Variable font axes | text-and-typography/use-variable-fonts.html | P1 | SkParagraph fontVariations | M4 | Implemented (Type settings shows a Variable group for text in a variable font: a slider and a value for each axis the font exposes (weight, width, optical size, slant, italic and custom axes such as grade; axes the font hides are left out), applied to the selected characters or whole layers, with Reset returning an axis to its default — the weight axis to the style's weight. Dragging a slider is one undo step. Axis values are stored per character and applied as SkParagraph font variations; the bundled Inter has a weight axis from 100 to 900, and uploaded or installed TrueType/OpenType variable fonts expose the axes of their `fvar` table, named from the font. Not included: axes of compressed WOFF/WOFF2 uploads (their tables can't be read without decompressing), the hover preview, the frequently used axis settings in the style menu, and replacing static shared fonts with variable versions (a team-library feature)) | src/core/text/font-axes.test.ts, src/engine/text/text-shaper.test.ts, src/editor/commands/text.test.ts, e2e/variable-fonts.spec.ts |
| Right-to-left text, bidi | text-and-typography/add-right-to-left-text.html | P1 | SkParagraph (ICU) | M4 | Implemented (each paragraph's direction is detected from its first letter and can be set left to right or right to left: Left to right / Right to left buttons in Typography once the text contains right-to-left script, the Text main menu, and the command palette ("Use right to left text direction"). Bidirectional text is reordered by SkParagraph with the paragraph's base direction; carets follow the glyph direction, selection highlights follow the runs, ← and → move backward and forward in reading order, and right-to-left list items are indented from the right with their markers on that side. Hebrew and Arabic characters fall back to bundled Noto Sans Hebrew and Noto Sans Arabic (variable weight) when the font lacks them. Alignment stays as set (left by default), as in the reference. Upgrading text changed by RTL plugins doesn't apply) | src/core/text/direction.test.ts, src/engine/text/text-shaper.test.ts, src/editor/commands/text.test.ts, src/editor/interactions/text-edit.test.ts, e2e/rtl.spec.ts |
| CJK text | text-and-typography/add-text-in-chinese-japanese-and-korean.html | P1 | Local fonts | M4 | Implemented (Chinese, Japanese and Korean characters fall back to bundled Noto Sans SC, TC, JP and KR (variable weight): text with kana uses Japanese, Hangul Korean, and other Han text Simplified Chinese, and picking one of the four families in the font picker — where they are listed like other fonts — decides which regional forms shared characters take. Each font is split into about 100–124 subsets by character range; only the subsets covering the characters in use load, as separate chunks the first time they're needed, and a layer lists only its script's loaded subsets as fallbacks so layout stays fast. Latin characters in a layer set to a Noto CJK family are drawn by Inter. Auto-sized text refits once its subsets load, without an undo step. Subsets are cached for offline use when first loaded, so a script works offline after it has been used online once) | src/core/text/cjk.test.ts, src/engine/text/text-shaper.test.ts, e2e/cjk.spec.ts |
| Emoji and smart symbols | text-and-typography/add-emojis-and-smart-symbols-to-text.html | P2 | Text input | M4 | Implemented (emoji draw in color from a bundled Noto Color Emoji fallback, loaded as a separate chunk the first time any text contains emoji; flags, skin tones and joined sequences shape as single glyphs. Typing ":" and at least two characters of a name while editing lists matching emoji by shortcode, name or keyword (English emojibase data with GitHub shortcodes, loaded on first search): ↑/↓ highlight, Return or Tab (or a click) inserts, Esc dismisses. The system emoji picker (⌃⌘Space) works through the text input. Preferences › Use smart quotes/symbols (off by default, per device) converts -> → ←, <- ←, vv ↓, ^^ ↑, (c) ©, (r) ®, (tm) ™, [ ] ▢, and straight quotes to curly opening or closing quotes as they are typed. The reference draws Apple-style emoji, which can't be bundled; Noto Color Emoji is used instead) | src/core/text/emoji.test.ts, src/core/text/emoji-search.test.ts, src/core/text/smart-symbols.test.ts, src/editor/interactions/text-edit.test.ts, src/engine/text/text-shaper.test.ts, e2e/emoji.spec.ts |
| Text styles (create, apply, detach) | text-and-typography/create-and-apply-text-styles.html | P0 | Styles | M8 | Implemented (Styles in the document model, with commands: color, text, effect and layout guide styles are document-level style nodes with a name, description and values; a style is created from values or from a selected layer (and applied to the selection), applied to layers (a color style to fills or strokes, a text style to text layers, an effect style, a layout guide style to frames), detached (the layers keep the values), renamed, described, edited, duplicated (the copy directly after the original), reordered, grouped in folders (slash paths in style names: move to a folder, rename or ungroup a folder) and deleted (detaching it from its layers). Editing a style updates every layer it is applied to, and instances through their main components; changing a styled property on a layer directly detaches the style. In the right sidebar, Local styles (when nothing is selected) lists the file's styles by type and folder, creates styles, and has Edit style (name and description), Duplicate, Add new folder, Delete, folder Rename, Ungroup and Delete, multi-select and drag to reorder or move between folders; the Typography, Fill, Stroke, Effects and Layout guide sections have an Apply styles button opening a searchable style picker (descriptions on hover, Create style from the selection) and show the applied style with Detach style. Edit style also edits the style's values, which update every layer using it as they change: a color style's paints (color and opacity, add and remove), a text style's font family, weight or style, size, line height, letter spacing and paragraph spacing and indent, an effect style's shadows (X, Y, blur, spread, color) and blurs (radius), and a layout guide style's guides (type, size, count, gutter, offset)) | src/editor/commands/styles.test.ts, e2e/styles.spec.ts, e2e/styles-edit.spec.ts |
| Font picker with preview and filters | text-and-typography/browse-and-apply-fonts.html | P0 | Font registry | M4 | Implemented (the font family button in Typography opens a font picker: search by name, a filter remembered for the session — All fonts, In this file, Installed and uploaded, Variable fonts — and a list of families shown in their own typeface; hovering or ↑/↓ previews a family on the selection (or the selected characters) and Esc restores it; clicking or Return applies it as one undo step. Families no longer available are marked Missing. The reference's Popular and Google Fonts collections need its online catalog and are not offered offline) | src/core/text/document-fonts.test.ts, e2e/font-picker.spec.ts |
| Local fonts | text-and-typography/add-a-font-to-reference.html | P0 | Local Font Access API | M4 | Implemented with a browser limitation (Upload fonts… in the font picker reads TTF, OTF, TTC, WOFF and WOFF2 files — family and style from the name table, or for compressed files the engine's family name and the file name's style — and keeps them in IndexedDB for every local file, deduplicated by content hash. Show installed fonts lists the fonts installed on this device through the Local Font Access API, which only Chromium provides; a family's files are read and stored when it is picked. User fonts register with the text engine and CSS at startup and whenever added) | src/core/text/font-names.test.ts, src/editor/fonts/font-registry.test.ts, src/ui/fonts/import-fonts.test.ts, e2e/font-picker.spec.ts |
| Missing fonts dialog with replacement | text-and-typography/browse-and-apply-fonts.html | P1 | Font registry | M4 | Implemented (once the text engine has loaded, a missing fonts notification appears at the bottom of the navigation bar whenever text layers or mixed-style runs use a family, or a style of a family, that isn't available on this device; it opens Missing fonts, listing each font with the number of layers using it and a replacement family and style (defaulting to Inter in the nearest style). Replace fonts swaps them in every text layer and run as one undo step, keeping all other styles; the notification disappears when nothing is missing, including after the font is uploaded or installed. Text in a missing font is drawn with the fallback fonts meanwhile) | src/core/text/missing-fonts.test.ts, src/editor/commands/text.test.ts, e2e/missing-fonts.spec.ts |
| Icon fonts | text-and-typography/use-icon-fonts.html | P3 | Local fonts | M4 | Implemented (icon fonts are used like any font: upload the font file or pick an installed one, choose it in the font picker, and paste or type its icon characters (usually private-use code points copied from the icon library). The characters shape from the icon font itself. Verified with Font Awesome 7 Free (solid and regular). Styles of compressed WOFF/WOFF2 uploads come from the file name, with weight numbers named (fa-solid-900 → Black, fa-regular-400 → Regular), since their name table can't be read; TTF/OTF uploads use the font's own style names, such as Solid) | src/engine/text/text-shaper.test.ts, src/core/text/font-names.test.ts, e2e/icon-fonts.spec.ts |
| Text on a path | text-and-typography/guide-to-text-in-reference-design.html | P1 | RSXform text | M11 | Implemented (Text on a path in the Type tools: clicking a vector layer's path adds a text layer that follows it and starts editing, taking the path's place on the canvas — the two share a transform, so the path lays the text out — along with its fills and effects. The text is shaped as one line and each character is drawn where its place in that line falls along the path, turned to face the way the path goes, so kerning holds. Typography has Flip text orientation, which puts the text on the other side reading the other way, and Start, which moves it along the path. A handle on the path marks where the text starts; dragging it moves the text along the path, to the nearest point on it) | e2e/text-on-path.spec.ts |
| Typography shortcuts (size, weight, spacing, line height) | text-and-typography/explore-text-properties.html | P1 | Keymap | M4 | Implemented (for selected text layers, or the selected characters while editing: ⇧⌘. / ⇧⌘, font size ±1, ⌥⌘. / ⌥⌘, the next heavier or lighter available style, ⌥. / ⌥, letter spacing ±0.1, ⇧⌥. / ⇧⌥, line height ±1 (Auto becomes pixels first); ⌘B bold, ⌘I italic, ⌥U (Ctrl+U) underline, ⇧⌘X strikethrough. Mixed values step individually; comma and period match by physical key, so Option combinations work on macOS) | src/core/text/typography-steps.test.ts, src/editor/commands/text.test.ts, e2e/type-settings.spec.ts |

## 6. Fills, strokes, effects, color

| Feature | Documentation | Pri | Dependency | M | Status | Tests |
|---|---|---:|---|---|---|---|
| Multiple fills: add, remove, reorder, visibility, opacity | color-gradients-and-images/guide-to-fills.html | P0 | Paint model | M1 / M3 | Implemented (fills and strokes of every paint type: add, remove, visibility, opacity, color picker; reorder by dragging a row's handle or with ↑/↓ on the focused handle, one undo step) | src/engine/render/scene-renderer.test.ts, src/editor/commands/panel-logic.test.ts, src/core/collections/move-item.test.ts, e2e/effect-order.spec.ts |
| Color picker (HSB area, hue, alpha; Hex/RGB/CSS/HSL/HSB), document colors | color-gradients-and-images/update-fills-using-the-color-picker.html | P0 | UI primitives | M3 | Implemented (popover from every color swatch — fills, strokes, gradient stops, page background: saturation/brightness area with pointer and arrow keys, hue and alpha sliders, Hex/RGB/CSS/HSL/HSB fields, one undo step per session, Esc or outside click closes. The CSS field shows `rgba(…)` (or `color(display-p3 …)` in Display P3 files) including opacity, and accepts any CSS color — hex, rgb/rgba, hsl/hsla in comma or space syntax, `color(srgb|display-p3 …)`, named colors, `transparent` — converting between sRGB and P3 to the file's profile and setting opacity from its alpha. Document colors: swatches of the distinct visible solid fill and stroke colors in the file (with opacity), most used first, up to 48, collected when the picker opens; clicking one applies its color and opacity. Library colors, styles and variables arrive with M7) | src/core/color/format.test.ts, src/core/color/css-color.test.ts, src/core/color/document-colors.test.ts, e2e/color-picker.spec.ts |
| Contrast checker (AA/AAA) | color-gradients-and-images/update-fills-using-the-color-picker.html | P2 | Color utils | M3 | Implemented (Check color contrast in the color picker of a single layer's solid fills: WCAG ratio of the fill against the color behind the layer's center (page background plus visible solid fills painted below it, composited), category menu Auto / Large text / Normal text / Graphics, AA and AAA badges (AAA for text only). A failing badge adjusts the color to the nearest compliant lightness, within the picker's undo step. Auto treats every layer as a graphic until text layers exist (M4); gradients and images below are not sampled) | src/core/color/contrast.test.ts, e2e/contrast.spec.ts |
| Eyedropper (I) | color-gradients-and-images/sample-colors-with-the-eyedropper-tool.html | P1 | EyeDropper API + canvas sampling | M3 | Implemented (I starts the eyedropper: a loupe shows the rendered color and hex under the pointer; clicking applies it to the selected layers' top visible solid fill (strokes for lines) as one undo step and returns to Move. The color picker's eyedropper uses the browser EyeDropper API where available (Chromium, whole screen) and otherwise picks from the canvas in every browser. ⌃C on macOS, Tab to switch color models in the loupe, and sampling styles/variables (M8) are not implemented; sampling outside the browser needs the EyeDropper API) | src/editor/tools/eyedropper-tool.test.ts, e2e/eyedropper.spec.ts |
| Linear, radial, angular, diamond gradients; on-canvas gradient editing | color-gradients-and-images/use-gradients-as-a-fill-or-stroke.html | P0 | Renderer shaders | M3 | Implemented (all four gradient types on fills and strokes: CanvasKit linear/radial/sweep shaders and an SkSL diamond shader, mapped through `gradientTransform`; per-row paint type dropdown converting solid ↔ gradient, CSS preview swatch, stops editor with position, color and alpha, add, remove (minimum two) and flip; persists and undoes. On-canvas editing: clicking a gradient swatch shows the gradient line with round end handles (plus a width handle for radial, angular and diamond) and a square per stop. Dragging an end moves or rotates the gradient; the center of a non-linear gradient moves it whole. Dragging a stop moves it; clicking the line adds a stop. Esc, clicking away or selecting another layer ends editing, and each drag is one undo step) | src/core/color/paints.test.ts, src/core/color/gradient-handles.test.ts, src/editor/interactions/gradient-edit.test.ts, src/engine/render/gradient-render.test.ts, e2e/gradient.spec.ts, e2e/gradient-handles.spec.ts |
| Image fills: fill / fit / crop / tile, rotate, crop tool, adjustments (exposure, contrast, saturation, temperature, tint, highlights, shadows) | color-gradients-and-images/adjust-the-properties-of-an-image.html, crop-an-image.html | P0 | Blob store | M3 | Implemented (IMAGE paints on fills and strokes: bytes stored once per SHA-256 hash in the IndexedDB `images` store and shared by all local files, loaded lazily with a checkerboard until available; Fill, Fit and Tile modes with tile size %, Rotate 90°, choose or replace the image, thumbnail swatch, paint opacity; the paint type select converts any paint to an image placeholder. Adjustments: exposure, contrast, saturation, temperature, tint, highlights and shadows as −100–100 sliders (double-click resets one, Reset adjustments clears all), stored in `filters` and rendered by an SkSL shader wrapping the image shader that matches the reference math in core/image/adjustments.ts; the thumbnail swatch shows the unadjusted image. Crop tool: double-click an image layer, click Crop image, or pick Crop in the mode menu. The whole image shows faded with a dashed outline. Crop handles move the crop edges while the image stays put; dragging inside repositions the image; the image's corner handles scale it about the opposite corner. Return, Esc, clicking elsewhere or selecting another layer applies the crop; each drag is one undo step. ⌥ moves the opposite crop edge too; an aspect ratio menu (Free, Original, 1:1, 5:4, 4:5, 4:3, 3:4, 3:2, 2:3, 16:9, 9:16) reshapes the crop box to the largest centered box and keeps its proportions while dragging; Resize to fit makes the crop show the whole image; a Zoom slider scales the image about the crop center relative to the size that covers it. Image corners and edges resize the image keeping its aspect ratio, or freely along its own axes with Control; just outside an image corner, dragging rotates the image about its center, ⇧ in 15° steps. ⌘-drag (Ctrl-drag) on a handle of a selected image layer crops it directly without staying in crop mode) | src/core/image/crop-tools.test.ts, src/core/image/adjustments.test.ts, src/core/image/image-fit.test.ts, src/core/image/image-paint.test.ts, src/engine/render/image-render.test.ts, src/platform/idb/images.test.ts, src/editor/images/image-registry.test.ts, e2e/images.spec.ts |
| Pattern fills (source layer, tile, scale, spacing) | color-gradients-and-images/use-patterns-as-a-fill-or-stroke.html | P2 | Renderer | M3 | Implemented (Pattern paint type for fills and strokes: Select source picks a layer on the canvas (hint shown, Esc cancels, the pattern layer stays selected); tile type Grid / Horizontal hex / Vertical hex, scale, X/Y spacing and Left / Center / Right alignment. The pattern is live: it re-records its source every frame, so edits to the source show immediately. A missing source, a pattern of itself, or an ancestor loop draws nothing) | src/core/color/pattern.test.ts, src/engine/render/pattern-render.test.ts, src/editor/tools/layer-pick-tool.test.ts, e2e/pattern.spec.ts |
| Video / GIF fills | color-gradients-and-images/add-images-and-videos-to-designs.html | P2 | Media | M10 | Implemented (MP4, MOV and WebM files up to 300 MB add video fills: Place image (⇧⌘K), dropping or pasting them on the canvas creates a layer at the video's size, and placing one on a rectangle, ellipse, polygon or star puts it in the fill; the video and its first frame are stored with the file and in .openframe files, the canvas draws the first frame (Fill, Fit or Tile, like images), the Fill section shows the video with a player to play, pause and scrub it, its mode and Replace video, and layers with a video fill show the video icon in the Layers panel. In presentation view video fills play: the Prototype tab's Video section sets whether a video plays when its frame shows (Autoplay, on by default), starts over when it ends (Loop) and plays with sound (Sound; where the browser doesn't allow sound before the viewer interacts with the page, it plays muted); a video pauses where it is when its frame is no longer shown, and with autoplay continues from there when the frame shows again. Video triggers and actions and Reset video state run in presentation view (see Triggers, Actions and State management); a video fill crops on the canvas like an image (Crop in its mode menu, with the crop aspect ratio, Resize to fit and the zoom), and choosing Video for a fill asks for a video file and makes it that fill. Animated GIFs (image fills from GIF files) play in presentation view, looping as the file says, while the canvas shows their first frame; the Fill section and the Layers panel label them GIF, and image adjustments aren't offered for them. The Fill section's Frame field chooses which frame of an animated GIF the canvas shows (presentation view plays it from the start). The canvas labels the selected layer GIF in a pill next to its size. The Export section offers GIF for a layer filled with an animated GIF: that export is the original file at 1x, so its frame delays and loop count are kept) | src/core/document/image-references.test.ts, src/core/image/gif-frame.test.ts, src/editor/commands/images.test.ts, src/editor/commands/video.test.ts, e2e/video-fills.spec.ts, e2e/presentation-video.spec.ts, e2e/presentation-gif.spec.ts, e2e/gif-canvas-frame.spec.ts, e2e/gif-export.spec.ts, src/editor/images/animated-gif.test.ts |
| Blend modes (pass through + 19) on layers, fills and effects | color-gradients-and-images/apply-blend-modes-to-layers-fills-and-effects.html | P0 | Skia blend + SkSL blenders | M3 | Implemented (renderer supports all modes incl. SkSL plus-darker; "Layer blend mode" dropdown in Appearance with mixed-selection support, undo and persistence; fills and strokes: blend mode menu in the color picker for solids and in the settings of gradients and images; shadows: blend mode menu in the effect settings. Drop shadows with a non-normal mode blend with what is behind the layer, inner shadows with the layer content; plus darker falls back to normal for inner shadows, which blend inside an image filter) | src/engine/render/scene-renderer.test.ts |
| Selection colors (mixed selection) | color-gradients-and-images/view-and-adjust-colors-in-a-mixed-selection.html | P1 | Selection summary | M3 | Implemented (Selection colors section for several selected layers or layers with contents: each distinct solid color and gradient from visible fills and strokes of the selection and everything inside it appears once (images, hidden paints and masks excluded; color and opacity together identify a color). Editing a solid color or any color's opacity changes every usage as one undo step; the target button selects the layers using the color; See all when there are more than 8. Styles and variables in the list arrive with M8) | src/core/color/selection-colors.test.ts, e2e/selection-colors.spec.ts |
| Color profiles (sRGB, Display P3) | color-gradients-and-images/about-color-models.html | P2 | Renderer | M3 | Implemented (File › Color profile: sRGB or Display P3 per file, an undoable document setting saved with the file. P3 files interpret color values as Display P3: the canvas surface is created in the P3 color space and document colors are drawn as extended sRGB, so wide-gamut colors show on P3 displays; swatches use CSS color(display-p3 …); the eyedropper reads back in the file's space; the contrast checker converts to sRGB for WCAG. Changing the profile keeps the numeric values, as in the reference editor. A default profile for new files and export profiles arrive with export (M9)) | src/core/color/color-profile.test.ts, e2e/color-profile.spec.ts |
| Stroke: position inside/center/outside, weight, per-side weights | additional-properties/apply-and-adjust-stroke-properties.html | P0 | Stroke geometry | M3 | Implemented (position and weight; frames and rectangles get a Stroke sides select — all, top, right, bottom, left — with per-side weight fields, stored as `individualStrokeWeights` and collapsed back when all sides match; per-side strokes ignore corner radii and dashes for now) | src/engine/render/scene-renderer.test.ts, src/engine/render/stroke-render.test.ts, src/editor/commands/stroke-properties.test.ts, e2e/stroke.spec.ts |
| Dashes (dash, gap, dash cap, custom pattern), joins, miter angle, end caps incl. arrows | additional-properties/apply-and-adjust-stroke-properties.html | P0 | Stroke geometry | M3 | Implemented (line end caps incl. arrows done in M2; Stroke style solid/dashed with dash and gap lengths and dash cap (none, round, square), starting with a half dash; joins miter/bevel/round with miter angle; all persist and undo. Custom multi-segment dash patterns in the UI and caps on open vector paths arrive with M6) | src/engine/render/shapes-render.test.ts, src/engine/render/stroke-render.test.ts, src/editor/commands/stroke-properties.test.ts, e2e/stroke.spec.ts |
| Brush and dynamic strokes (frequency, wiggle, smoothen) | additional-properties/apply-and-adjust-stroke-properties.html | P2 | Draw | M11 | Implemented (Dynamic stroke in the Stroke section gives a stroke a hand-drawn, bumpy look: Frequency sets how many bumps it has, Wiggle how far they go, and Smoothen how rounded rather than jagged they are. The path is sampled and each point moves along its normal by an offset that follows the path's length, so a layer bumps the same way on every draw; turning it on centers the stroke, the only position a dynamic stroke takes. The Brush paints with one) | src/core/vector/dynamic-stroke.test.ts, e2e/sketch-toolbar.spec.ts |
| Drop shadow, inner shadow (X, Y, blur, spread, color, show behind transparent areas) | additional-properties/apply-effects-to-layers.html | P0 | Renderer | M3 | Implemented (Effects section: add, type, visibility, remove; X, Y, blur, spread, color and opacity via the color picker, and Show behind transparent areas for drop shadows; rendered with composed CanvasKit image filters; persists and undoes. Each shadow has its own blend mode) | src/core/effects/effects.test.ts, src/engine/render/effect-render.test.ts, e2e/effects.spec.ts |
| Layer blur, background blur (uniform and progressive) | additional-properties/apply-effects-to-layers.html | P0 / P1 | Backdrop saveLayer | M3 | Implemented (uniform and progressive layer blur and background blur, background blur clipped to the layer's shape. Progressive: Uniform/Progressive menu, start and end radius, start and end position fields, and on-canvas start/end handles; rendered as weighted blur levels that ramp smoothly. Background blur on a group covers the union of its visible children's shapes, and on a line its stroke) | src/core/effects/effects.test.ts, src/editor/interactions/blur-edit.test.ts, src/engine/render/effect-render.test.ts, e2e/effects.spec.ts, e2e/progressive-blur.spec.ts |
| Noise (mono/duo/multi), texture | additional-properties/apply-effects-to-layers.html | P2 | SkSL | M3 | Implemented (Noise: Mono / Duo / Multi, size, density, color(s) with opacity or multi opacity, blend mode; drawn by an SkSL noise shader over the layer's content only. Texture: size, radius and Clip to shape; roughens edges with a smooth-noise displacement map, spreading past the layer unless clipped. Up to two noise effects and one texture per layer; layer blur, noise and texture apply in list order; the Scale tool scales their sizes) | src/core/effects/effects.test.ts, src/engine/render/effect-render.test.ts, e2e/noise-texture.spec.ts |
| Glass effect | additional-properties/apply-effects-to-layers.html | P2 | SkSL + backdrop | M3 | Implemented as an approximation (Glass effect type with light angle, light intensity, refraction, depth, dispersion, frost and splay; one per layer. Inside the layer's shape the backdrop is frosted (blur), refracted near the edges by a displacement map driven by an edge-normal field (rounded box or ellipse), color-split by dispersion, and lit with a directional edge highlight. Glass and background blur on one layer render only the first in the list; glass behind glass isn't factored in. The optics are an approximation of the reference renderer, not a physical model) | src/core/effects/effects.test.ts, src/engine/render/effect-render.test.ts, e2e/glass.spec.ts |
| Effect limits, ordering, render order | additional-properties/apply-effects-to-layers.html | P1 | Renderer | M3 | Implemented (render order: background blur, then drop shadows under the content, inner shadows over it, layer blur over everything. Limits: 8 drop shadows, 8 inner shadows, one layer blur and one background blur per layer — Add effect picks a type with room and disables when full, types at their limit are disabled in the type menu, pasted effects respect the limits, and extra effects in files are ignored when rendering. Effects reorder by drag handle or ↑/↓. Noise (2 per layer) and texture (1) follow the same limits; layer blur, noise and texture apply on top in list order. Glass is limited to one per layer, and with a background blur only the first of the two renders) | src/core/effects/effects.test.ts, src/engine/render/effect-render.test.ts, e2e/effect-order.spec.ts |

## 7. Auto layout

| Feature | Documentation | Pri | Dependency | M | Status | Tests |
|---|---|---:|---|---|---|---|
| Add / remove / suggest auto layout (⇧A, ⌥⇧A, ⌃⇧A) | use-auto-layout/toggle-on-auto-layout-in-designs.html | P0 | Layout engine | M5 | Implemented (⇧A, Object menu and the layer context menu: a frame gets auto layout in place — the flow direction follows how its children are arranged, the gap is their average spacing, padding is the space around them, children lined up on their centers or far edges keep that alignment, layers are reordered to their position along the flow, and a frame with children hugs them; any other selection is wrapped in a new auto layout frame without a fill. ⌥⇧A (when the selection has auto layout; otherwise it still aligns left to the parent) and Freeform in the Layout section remove it, leaving children in place. Suggest auto layout (⌃⇧A on macOS, Ctrl+Alt+Shift+A elsewhere, Object menu and layer context menu) gives a frame and the frames inside it as much auto layout as their arrangement allows while keeping the design in place: a single row or column flows directly, rows (or columns) of several layers are wrapped in new fill-less auto layout frames, and overlapping layers are left as they are; other selections are wrapped in a frame first. Frames it creates or converts inside the selection are marked with a blue dot in the layers panel until they are selected) | src/core/layout/auto-layout.test.ts, src/core/layout/suggest-auto-layout.test.ts, e2e/auto-layout.spec.ts, e2e/auto-layout-suggest.spec.ts |
| Vertical and horizontal flow, wrap with separate gaps | use-auto-layout/use-the-horizontal-and-vertical-flows-in-auto-layout.html | P0 | Layout engine | M5 | Implemented (Vertical and Horizontal buttons in the Layout section; children flow in layer order, hidden layers are left out; horizontal flows can Wrap, with a gap between rows, when the width is fixed. Changes to a frame's settings or size, or to its children, lay it out again at every commit and live while resizing, nested frames included) | src/core/layout/flow-layout.test.ts, src/core/layout/auto-layout.test.ts, e2e/auto-layout.spec.ts |
| Padding (on-canvas handles, shorthand), gap, auto gap (between/around/evenly), negative gap | use-auto-layout/use-the-horizontal-and-vertical-flows-in-auto-layout.html | P0 | Layout engine | M5 | Implemented (horizontal and vertical padding fields, or top/right/bottom/left with Individual padding; gap between items as a number (negative overlaps) or Auto — between, around or evenly — which never goes below 0. CSS shorthand in any padding field ("1,2,3,4" is top, right, bottom and left; "1,2" and "1,2,3" as in CSS), and ⌘-click (Ctrl-click) on a padding field makes the value apply to every side. On the canvas, hovering the selected auto layout frame shows pink handles in the middle of each padding band and between children (fixed gaps in unwrapped flows): dragging a padding handle inward grows that side (⌥ both opposite sides, ⌥⇧ all sides, ⇧ in big nudge steps), and dragging a gap handle along the flow changes the gap. Clicking a handle without dragging opens a field next to it to type the value — ⌥-click for both opposite sides, ⌥⇧-click for all sides; Return applies, Esc or leaving the field closes it) | src/core/layout/flow-layout.test.ts, src/ui/panels/inspector/padding-shorthand.test.ts, src/core/layout/layout-handles.test.ts, e2e/auto-layout.spec.ts, e2e/auto-layout-keys.spec.ts, e2e/auto-layout-handles.spec.ts, e2e/auto-layout-handle-values.spec.ts |
| Alignment box (9 positions / 3 positions), baseline alignment | use-auto-layout/use-the-horizontal-and-vertical-flows-in-auto-layout.html | P0 | Layout engine | M5 | Implemented (nine positions with a fixed gap, three across the flow with an Auto gap; wrapped rows align as a block. In the focused box, arrow keys move the alignment one step, W/A/S/D move it to an edge, and X toggles the Auto gap, without triggering tool shortcuts. Text baseline alignment (Auto layout settings, or B in the box) lines up the first baselines of text layers in horizontal flows, with other layers sitting on their bottom edge) | src/core/layout/flow-layout.test.ts, src/core/layout/auto-layout.test.ts, e2e/auto-layout.spec.ts, e2e/auto-layout-keys.spec.ts, e2e/auto-layout-baseline.spec.ts |
| Hug / fill / fixed sizing, min/max width and height | use-auto-layout/guide-to-auto-layout.html | P0 | Layout engine | M5 | Implemented (Width sizing and Height sizing: Hug contents for auto layout frames and text (mapped onto auto width and auto height), Fill container for children of auto layout frames — sharing the free space by content area and stretching across the flow — and Fixed; typing a size or dragging a handle makes that axis fixed, and filling along a hugging parent's flow makes the parent fixed. Rotated children flow by their bounding box and keep their size. Min and max width and height for auto layout frames and their children (Add min/max and Remove min and max in the sizing selects, with fields for set limits; a frame never shrinks below its padding; a text layer's max height and max lines exclude each other). Double-clicking a selected layer's edge handle sets that axis to Hug contents, and ⌥ double-click to Fill container, where they apply) | src/core/layout/flow-layout.test.ts, src/core/layout/auto-layout.test.ts, e2e/auto-layout.spec.ts, e2e/auto-layout-edges.spec.ts |
| Ignore auto layout (absolute position) | use-auto-layout/guide-to-auto-layout.html | P0 | Layout engine | M5 | Implemented (Ignore auto layout in the Position section for children of auto layout frames: the child leaves the flow and keeps its position, its siblings close the gap, fill resizing is dropped, and its Horizontal and Vertical constraints apply when the frame resizes. Holding ⌃ while dropping layers into an auto layout frame adds them this way at the drop position, without an insertion line) | src/core/layout/auto-layout.test.ts, e2e/auto-layout-settings.spec.ts, e2e/auto-layout-ignore-drag.spec.ts |
| Canvas stacking, strokes included in layout | use-auto-layout/use-the-horizontal-and-vertical-flows-in-auto-layout.html | P1 | Layout engine | M5 | Implemented (Auto layout settings: Canvas stacking — Last on top or First on top, which reverses drawing and hit testing without changing the layer order — and Inside strokes included in or excluded from layout; only inside strokes ever count, and each frame's setting affects only that frame) | src/core/layout/auto-layout.test.ts, e2e/auto-layout-settings.spec.ts |
| Grid flow: tracks (fixed, fr, hug, min/max), row and column gaps, spans, cell alignment, auto-placement | use-auto-layout/use-the-grid-auto-layout-flow.html | P1 | Layout engine | M5 | Implemented (Grid layout button: the number of columns and the gaps come from how the children are arranged, and they are reordered to reading order. Number of columns, Number of rows or Auto rows, a size per column and row — Fixed, Hug contents or Fill (fr, sharing the free space; hugging in a hugging frame) — gaps between columns and rows, padding, and Automatic positioning (off: children keep their cells, so empty cells stay; back on: rows return to Auto). Children get Column span and Row span and align in their cells (left, center, right, top, middle, bottom); fill children stretch over their cells. Drag and arrow keys reorder cells in automatic positioning (↑/↓ move a row). While the selected grid frame is hovered, its tracks show pills with their sizes (px, fr or Hug) along the top and left sides, and dragging a track's edge near those sides makes the track fixed at its new size. A track's size pill picks that track out on the canvas (⇧ picks more), washing it over so what is being worked on is plain; dragging a pill carries its track to another place in the grid, the children travelling with it and the ones stepped over shifting the other way, and Delete takes the track away — the children in it going back to being placed automatically, the ones after it moving up a track and the ones reaching over it reaching one track less. A grid always keeps one track. With Automatic positioning off, dragging a child inside the grid puts it in the cell under the pointer, empty or not, the nearest track winning when the pointer is in a gap or outside them. Every track also carries a smallest and a largest size, which hold it whatever its type works out to) | src/core/layout/grid-layout.test.ts, src/core/layout/auto-layout.test.ts, src/core/layout/grid-track-handles.test.ts, src/editor/interactions/grid-tracks.test.ts, e2e/auto-layout-grid.spec.ts, e2e/auto-layout-grid-tracks.spec.ts |
| Reorder children by drag or arrow keys with insertion indicator | use-auto-layout/use-the-horizontal-and-vertical-flows-in-auto-layout.html | P0 | Interactions | M5 | Implemented (dragging children of an auto layout frame shows a blue insertion line between their siblings — in a wrapping flow, on the row under the pointer — while the other children stay laid out, and dropping places them there; dragging a layer from elsewhere into an auto layout frame inserts it at the pointer; arrow keys along the flow move selected children one position (arrows across the flow do nothing); ⌘D puts the duplicate right after the original. Deep-selecting nested children with ⌘ works as elsewhere) | src/core/layout/flow-order.test.ts, e2e/auto-layout-reorder.spec.ts |
| Layout version behaviors (border-box padding, inside strokes only) | use-auto-layout/use-auto-layout-with-css-flexbox-in-mind.html | P1 | Layout engine | M5 | Implemented (the new version only: a frame is never smaller than its padding plus inside strokes, even past its max size; only inside strokes take up room; fill children share space by content area; Auto gaps never go below 0; a single child of a Between stack starts at the start, while Around and Evenly center it) | src/core/layout/flow-layout.test.ts |

## 8. Components, instances, variants

| Feature | Documentation | Pri | Dependency | M | Status | Tests |
|---|---|---:|---|---|---|---|
| Create component (⌥⌘K), create multiple components, restore deleted main component | components/create-components-to-reuse-in-designs.html | P0 | Instance resolver | M7 | Implemented (⌥⌘K, the Object menu, the layer context menu or the Create component button next to the selection's type in the properties panel: a single selected frame becomes a component itself, and any other selection (an instance included) is nested in a new component frame without a fill, named "Component N". Components show the component icon in the layers panel and are labeled Component in the properties panel; one undo step, and they persist. Restore main component rebuilds a deleted main component from a selected instance, next to it, and links the instance to it again. Create multiple components, in the Create component options menu next to the Create component button when two or more layers are selected (and in the Object menu), makes a component of each selected layer as one undo step: a frame becomes the component itself, any other layer (group, boolean group, path or shape) is nested in its own component frame without a fill, and main components are left as they are; the new components are selected. Components and component sets are identified by the purple Component icon in the layers panel and Find results) | src/editor/commands/components.test.ts, e2e/components.spec.ts, e2e/multiple-components.spec.ts, e2e/component-icon.spec.ts |
| Instances: insert, duplicate, go to main (⌃⌥⌘K), push overrides to main | use-libraries/create-and-insert-component-instances.html | P0 | Instance resolver | M7 | Implemented (Duplicating (⌘D), ⌥-dragging or copying and pasting a main component creates an instance of it, labeled Instance with the instance icon. Instances follow their main component: a change on one of its layers reaches the matching layer of every instance, and resizing it resizes instances that weren't resized; each instance keeps its own position. Changes to an instance's fills, strokes, effects, text, name and visibility are kept as overrides when the main component changes; moving or resizing layers inside an instance is reverted. Go to main component (⌃⌥⌘K) selects and zooms to the main component of the selected instance or a layer inside it; Push changes to main component copies the selected instance's changes to the main component, which passes them to its other instances. Layers added to, deleted from, reordered or moved within a main component (or moved into or out of it) are added, deleted and moved in its instances, and through nested instances in theirs, in the same undo step. Instances follow while a main component or its layers are dragged, moved or resized, before the change is committed, and go back if the drag is cancelled) | src/core/document/instance-fields.test.ts, src/editor/commands/instances.test.ts, e2e/instances.spec.ts, src/editor/commands/main-component.test.ts, e2e/main-component.spec.ts, src/editor/commands/structure-sync.test.ts, e2e/structure-sync.spec.ts, src/editor/commands/live-instances.test.ts |
| Overrides (allowed vs disallowed), reset one or all overrides | use-libraries/apply-changes-to-instances.html | P0 | Override paths | M7 | Implemented (Allowed overrides — name, visibility, fills, strokes, effects, layout guides, text properties and prototype interactions — are kept on an instance when its main component changes; moving, resizing, reordering and constraint changes inside an instance are reverted. Reset all changes (the properties panel button, Object menu or layer context menu) gives a selected instance, or a layer inside one, the main component's values back, and changing a field back to the main component's value stops overriding it. Instances nested in components follow through every level: a change to the inner component reaches its instances inside other components and their instances, a change to a nested instance inside a component is kept there as a change and reaches that component's instances, and moving or renaming the nested instance in its component updates its copies; changes made on the outer instance are kept, and Reset all changes brings back the outer component's values. The More actions menu next to the type in the right sidebar lists the changed properties of a selected instance (or of a selected layer inside one) under Reset, to reset one property (Reset fill, Reset name…) keeping the other changes, or all changes) | src/core/document/instance-fields.test.ts, src/editor/commands/instances.test.ts, src/editor/commands/reset-overrides.test.ts, src/editor/commands/reset-override.test.ts, src/editor/commands/nested-instances.test.ts, e2e/reset-overrides.spec.ts, e2e/more-actions.spec.ts |
| Detach instance (⌥⌘B) | use-libraries/detach-an-instance-from-the-component.html | P0 | Instance resolver | M7 | Implemented (⌥⌘B, the Object menu or the layer context menu: each selected instance becomes a regular frame that keeps its current layers and properties, without its link to the main component, so later changes to the component no longer reach it; one undo step. The More actions menu next to the type in the right sidebar detaches the selected instance too) | src/editor/commands/detach.test.ts, e2e/detach.spec.ts, e2e/more-actions.spec.ts |
| Swap instance while preserving overrides; preferred instances; quick insert (⇧I) | manage-your-libraries/swap-components-and-instances.html | P1 | Instance resolver | M7 | Implemented (the instance menu in the Component section of the right sidebar lists the file's components; choosing one rebuilds the instance from that component under the same layer, keeping its position and the changes made on the instance frame and on layers whose name and type match in the new component, as one undo step. Dropping a component from the Assets tab onto an instance while holding ⌥ (Alt) swaps that instance when it isn't nested in a frame or component, and ⌥⌘ (Alt+Ctrl) swaps a nested one; without the modifier the drop inserts a new instance. Quick insert (⇧I) implemented. Instances nested in components are swapped the same way: swapping the copy of a nested instance inside an instance keeps it in place as that instance's own change (later changes to the nested instance in the component don't undo it, moving it there still moves the copy, and Reset all changes swaps it back), and swapping a nested instance inside a main component swaps its copies in that component's instances, keeping their changes, except the copies that were swapped themselves. Right-clicking a single instance on the canvas or in the layers panel shows Swap instance, listing the related components (the main components in the same frame, page or component set as its main component) with its own checked. Preferred instances are set on instance swap properties and listed first in the property's dropdown on instances) | src/editor/commands/swap-instance.test.ts, src/editor/commands/nested-instances.test.ts, e2e/swap-instance.spec.ts, e2e/swap-instance-drop.spec.ts, e2e/swap-instance-menu.spec.ts |
| Variants, component sets, combine as variants, conflict errors | components/create-and-use-variants.html | P0 | Component sets | M7 | Implemented (Combine as variants, in the properties panel when two or more main components sharing a parent are selected (and in the Object menu), puts them in a component set without moving them, as one undo step: the set has a dashed purple stroke and no fill, is labeled Component set and shows the component icon in the layers panel. Using the slash naming convention, the text before the first / names the set and the other parts become values of properties named Variant, Property 2, Property 3…, so Button/Primary/Large becomes Variant=Primary, Property 2=Large; a name without a slash is the Variant value as a whole. The set's properties and values are read from its variants' names, the variant in the top-left corner is the default, and variants sharing a combination of values (conflicted) or not following the Property=value syntax (corrupted) are detected. An instance of a variant is named after its component set (renaming the set renames the instances that weren't renamed, and Reset all changes goes back to the set's name) and shows the set's properties under the instance menu in the right sidebar: a dropdown of values for each property, or a toggle for a property whose values are true and false. Choosing a value swaps the instance for the variant with that combination of values, or else the closest one with the chosen value, keeping its position and changes, as one undo step. A component set is listed once in the Assets tab, quick insert and the instance menu, named after the set and inserted as its default variant; ⌘D (or ⌥-drag) on a variant duplicates it as a new variant of the set rather than making an instance. A component set's Properties section in the right sidebar lists its variant properties with their values: double-click a property to rename it, drag properties to reorder them, open a property's values to change them, and right-click a property (or press Delete) to delete it; the variants' names follow each change as one undo step, and deleting a set's only property deletes the set. Conflicting and corrupted variants are reported in that section, and component sets take a description and documentation link like components. Add variant (the + next to the selection's type in the right sidebar, or the Object menu) on a main component on its own makes an identical component below it and puts both in a new component set; on a component set it adds a copy of the bottom variant below the others (on a selected variant, a copy of that variant), 20 px apart in a single column, growing the set; one undo step, with the new variant selected. A selected component set also shows a purple + button just below it on the canvas, which adds a variant the same way) | src/core/document/variants.test.ts, src/editor/commands/variants.test.ts, src/editor/commands/variant-properties.test.ts, src/editor/commands/add-variant.test.ts, e2e/add-variant.spec.ts, e2e/variants.spec.ts, e2e/instance-variants.spec.ts, e2e/variant-properties.spec.ts, src/editor/chrome/add-variant-button.test.ts, e2e/add-variant-canvas.spec.ts |
| Component properties: boolean, text, instance swap, variant, nested exposure | components/explore-component-properties.html | P0 | Property defs | M7 | Implemented (boolean and text component properties in the document model, with commands: create a property with a name (unique among the component's and component set's properties) and a default value on a main component or component set; apply it to a layer of the component or of a variant, where boolean properties drive the layer's visibility and text properties the text of text layers, and the layer takes the default value; changing the default updates the bound layers of the component, and through them the instances that weren't changed; renaming and deleting update the bound layers; on an instance, setting a property changes its bound layers as changes on the instance, and Reset all changes brings back the defaults. Variant properties are implemented with variants. In the right sidebar, the Properties section of a main component or component set creates properties (the + button, Boolean or Text, then a name and default value), changes their defaults, renames them (double-click) and deletes them (right-click); a layer nested in the component takes a boolean property from the Appearance section header and a text layer a text property from the Typography section header; an instance shows a toggle or text field for each property under the instance menu. Instance swap properties are in the document model with commands: created with a default component and preferred components, applied to an instance nested in the component (which is swapped to the default); changing the default swaps the bound nested instances in the component and, through them, in its instances that didn't swap them; setting the property on an instance swaps its nested instance as a change on that instance, which Reset all changes swaps back. In the right sidebar, an instance swap property is created from the Properties section (Instance swap, then a name, the default component and preferred components), its default and preferred components are changed from its row, a nested instance is bound to it from the Instance swap property dropdown at the top of the right sidebar, and an instance swaps its nested instance from the property's dropdown (marked with the instance icon), which lists the preferred components first and then all components. Nested instances are exposed from the Properties section of a main component or component set (the + button, Nested instances, then a checklist of the nested instances whose components have properties or variants), listed there with Remove to stop exposing them; an instance then shows each exposed nested instance's variant and component property controls under its own, except for nested instances that are hidden, and hovering those controls highlights the nested instance on the canvas. A boolean or text property's default value takes a variable from Apply variable next to the property in the Properties section (a boolean or string variable for boolean properties, a string or number variable for text properties); the default follows the variable in the component's variable modes, and so do the bound layers and the instances that haven't changed them; editing the default detaches the variable, and Detach variable keeps the current default) | src/editor/commands/component-properties.test.ts, src/editor/commands/instance-swap-property.test.ts, src/editor/commands/expose-instances.test.ts, e2e/component-properties.spec.ts, e2e/instance-swap-property.spec.ts, e2e/expose-instances.spec.ts |
| Slots (convert to slot, min/max layers, preferred instances, reset slot) | components/use-slots-to-build-flexible-components-in-reference.html | P1 | Instance resolver | M7 | Implemented (Slot properties in the document model, with commands: Convert to slot (⌘⇧S, or the Object and right-click menus) makes a frame nested in a main component or variant a slot, bound to a new slot property (Slot, Slot 2…) or an existing one, with the frame's layers as its default content; Wrap in new slot puts the selected layers of a component in a new slot frame without moving them; a slot property's settings are its description, minimum and maximum layer counts, preferred instances, Only allow preferred instances, Display empty slots by default and Fill items on the counter-axis by default; deleting the property keeps the frame and its contents. In an instance, layers can be drawn, pasted or dragged into a slot, and the slot's content moved, resized and deleted as in a regular frame; the slot is then marked as changed, and the main component's changes to the slot's content no longer reach it, while untouched instances keep following. Elsewhere in an instance, new, pasted and dragged layers go beside the instance rather than into it, and the instance's own layers can't be dragged out of it. Reset slot (under Reset in More actions, on the slot or its instance, or Reset all changes) brings back the main component's slot content, which the slot then follows again; Delete contents (More actions on a slot of an instance) removes every layer from it; a changed slot's property is tagged Modified on the instance. In the right sidebar, Slot in the Create component property menu creates a slot property; Convert to slot at the top of the sidebar makes the selected frame nested in a component a slot of a new or an existing slot property; and Edit slot property on the slot property's row edits its description, minimum and maximum layers, preferred instances, Only allow preferred instances, By default, display empty slots and By default, fill items on slot's counter-axis. On an instance, each slot property shows Modified once its content changed, Add instances, which lists the file's components (only the preferred instances by default when the slot has some) and inserts the chosen one into the slot, filling its counter-axis when the slot is set to, and Limits, listing each minimum, maximum and preferred-instances rule as met or not (orange while one isn't), with View layers selecting the layers that aren't preferred instances; a change that takes a slot of an instance past its limits shows a warning, and the slot can still be edited. The canvas marks the slots of the hovered instance, and empty slots set to show, with a pink box. Hovering an instance on the canvas shows Add instances in the top-left corner of each of its slots, which opens the same list) | src/editor/commands/slots.test.ts, src/editor/commands/slot-content.test.ts, src/editor/commands/slot-reset.test.ts, e2e/slot-settings.spec.ts, src/editor/commands/slot-limits.test.ts, e2e/add-instances.spec.ts, src/editor/chrome/add-instances-button.test.ts, e2e/add-instances-canvas.spec.ts |
| Interactive components (change to, variant interactions) | components/create-interactive-components-with-variants.html | P1 | Prototype runtime | M10 | Implemented (a layer in a variant of a component set, or in an instance of one, can have Change to interactions — the action is offered in the interaction details there, with the set's variants as destinations, and dragging the + from a variant onto another variant of its set connects them; instances carry their variant's interactions; in presentation view and inline preview a Change to switches the instance to the destination variant (keeping the instance's changes on matching layers), the prototype remembers the switched variants while you navigate, and Restart (or an edit, inline) puts them back, as does an interaction's Reset component state for the instances in its destination; the file isn't changed. Navigating between frames that share states switches the destination's matching instance to the variant its match was switched to (see State memorization). An instance lists the interactions it takes from its set in a Variant interactions section of the Prototype tab, to view rather than edit (they are changed on the main component, or an interaction added here gives the instance its own). A Change to animates: Instant, Dissolve or Smart animate, over the interaction's duration and easing — the instance's layers are matched between the two variants by their path of names, so a matched layer moves, resizes and blends its colors (Dissolve blends the colors where they are), a layer the new variant adds fades in and one the old variant had fades out where it was) | src/editor/prototype-runtime.test.ts, src/core/prototype/reset-state.test.ts, e2e/interactive-components.spec.ts, e2e/interactive-components-reset.spec.ts, src/core/prototype/state-sharing.test.ts, e2e/interactive-components-sharing.spec.ts, e2e/interactive-components-animation.spec.ts, src/core/prototype/variant-animate.test.ts |
| Animated components | components/create-and-use-animated-components.html | P2 | Motion | M12 | Implemented (layers animated in Motion become a component in Design like any other, and every instance then runs the component's animation on its own layers, mapped through the layer each of them mirrors. An animated instance has a track of its own on the timeline, in the component's purple, which is dragged along to delay or advance its animation; its length and its keyframes belong to the main component and are not edited from the instance, and its component properties are only editable back in Design. The Motion preview is left out of the reconciling between a component and its instances, so an instance can stand where the animation puts it) | src/editor/commands/animated-components.test.ts, e2e/animated-components.spec.ts |
| Assets panel (⌥2): grid/list, search, drag to insert, component details/playground | use-libraries/create-and-insert-component-instances.html | P0 | Components | M7 | Implemented (⌥2 or the Assets tab in the navigation bar shows this file's main components, from every page, searchable by name, in place of the pages and layers; clicking one opens its details, and dragging one onto the canvas inserts it at the drop point; each insert is one undo step and selects the instance. Quick insert (⇧I) opens the actions menu on the file's components: typing filters them by name and Enter inserts an instance. Components are listed in folders: their page, the sections and frames they're in, then each part of their name before a slash (Button/Primary is listed as Primary in Button); turning off Show sub-folders lists them flat under their full names. The Grid view shows each component as a thumbnail the rendering engine draws from its current state, with its name, and List shows the names. The component details show a preview of the component's default state (which can be dragged onto the canvas), its description and documentation link, and where it lives, with Insert instance, which inserts it next to its main component. The reference offers the component playground on library components, so it comes with library snapshots (M14); team libraries aren't available offline) | src/editor/commands/insert-instance.test.ts, e2e/assets.spec.ts, e2e/quick-insert.spec.ts, src/editor/commands/assets.test.ts, e2e/assets-folders.spec.ts, e2e/assets-grid.spec.ts, e2e/component-details.spec.ts |
| Descriptions and documentation links for components, styles, variables | create-and-share-libraries/add-descriptions-to-styles-components-and-variables.html | P1 | Document model | M7 / M8 | Implemented (Components: a main component's description and documentation link are edited in the properties panel's Component section (The reference opens a Component configuration dialog for them) and persist; instances show their main component's description and an Open documentation link (http and https links only). The Assets tab and Quick insert search descriptions as well as names. Component sets take a description and documentation link the same way, and variants as the components they are. Styles take a description in Create style and Edit style, shown when hovering the style in the style picker and in Local styles; variables take one in Edit variable, shown when hovering them in the variable pickers) | src/editor/commands/components.test.ts, e2e/component-description.spec.ts |

## 9. Styles, variables, libraries

| Feature | Documentation | Pri | Dependency | M | Status | Tests |
|---|---|---:|---|---|---|---|
| Color, text, effect and layout guide styles: create, apply, edit, detach, folders, reorder, duplicate, delete | styles/*.html, use-libraries/apply-styles-to-layers-and-objects.html | P0 | Style store | M8 | Implemented (Styles in the document model, with commands: color, text, effect and layout guide styles are document-level style nodes with a name, description and values; a style is created from values or from a selected layer (and applied to the selection), applied to layers (a color style to fills or strokes, a text style to text layers, an effect style, a layout guide style to frames), detached (the layers keep the values), renamed, described, edited, duplicated (the copy directly after the original), reordered, grouped in folders (slash paths in style names: move to a folder, rename or ungroup a folder) and deleted (detaching it from its layers). Editing a style updates every layer it is applied to, and instances through their main components; changing a styled property on a layer directly detaches the style. In the right sidebar, Local styles (when nothing is selected) lists the file's styles by type and folder, creates styles, and has Edit style (name and description), Duplicate, Add new folder, Delete, folder Rename, Ungroup and Delete, multi-select and drag to reorder or move between folders; the Typography, Fill, Stroke, Effects and Layout guide sections have an Apply styles button opening a searchable style picker (descriptions on hover, Create style from the selection) and show the applied style with Detach style. Edit style also edits the style's values, which update every layer using it as they change: a color style's paints (color and opacity, add and remove), a text style's font family, weight or style, size, line height, letter spacing and paragraph spacing and indent, an effect style's shadows (X, Y, blur, spread, color) and blurs (radius), and a layout guide style's guides (type, size, count, gutter, offset)) | src/editor/commands/styles.test.ts, e2e/styles.spec.ts, e2e/styles-edit.spec.ts |
| Variables view: collections, groups, create/rename/duplicate/delete, search and filter | variables/create-and-manage-variables-and-collections.html | P0 | Variable store | M8 | Implemented (the Variables tab of the navigation bar opens the variables view over the canvas (Escape or Close closes it): collections in a sidebar (create, select, double-click or right-click to rename, Import modes, Export modes, Delete collection); the selected collection's variables in a table with a column per mode, searchable by name, group or value and filterable by type, with slash-named variables grouped under group rows; Create variable (color, number, string, boolean); values edited in place (color and opacity with the color picker, numbers, strings, a checkbox for booleans); right-click a value for Create alias (a picker of same-type variables that don't make a cycle) and Detach alias; mode columns can be renamed (double-click), duplicated, set as default, moved left or right, imported or exported as DTCG JSON, and deleted; New variable mode adds a column; variables are selected with click, ⇧-click and ⌘-click, renamed by double-click, duplicated with ⇧Return, deleted, and edited in Edit variable (name, description, scopes, code syntax). Minimize shrinks the view into a window in the corner of the canvas, resized from its edges, and Expand restores it; variables are dragged to reorder them (joining the group of the variable they're dropped next to); Copy variable (⌘C) and Paste variables (⌘V, or a collection's context menu) copy variables into any collection, with values matched by mode name (else the default mode's value), unique names, and aliases kept when their target is in the file) | src/editor/commands/variables.test.ts, src/editor/commands/variables-clipboard.test.ts, e2e/variables-view.spec.ts, e2e/variables-organize.spec.ts |
| Types: color, number, string, boolean (plus timing and easing for Motion) | variables/overview-of-variables-collections-and-modes.html | P0 | Variable store | M8 / M12 | Implemented (color, number, string, boolean and easing variables in collections, as document nodes: create (named by type, unique in the collection), rename, set values per mode, duplicate directly after the original, delete (bound properties keep their values), descriptions, scopes (Show in all or per property) and code syntax for Web, Android and iOS; easing came with M12, and timing is a number variable bound to the animation's length from the timeline, looked up when the animation is read so the same length can be shared) | src/editor/commands/variables.test.ts, src/core/variables/resolve.test.ts, e2e/variables-binding.spec.ts, e2e/variables-sidebar.spec.ts |
| Aliases (same type), cycle detection | variables/create-and-manage-variables-and-collections.html | P0 | Resolver | M8 | Implemented (a variable's value in a mode can alias another variable of the same type, in any collection, resolved in the mode of the target's collection; aliases that would make a cycle are refused; Detach alias keeps the value the alias had) | src/editor/commands/variables.test.ts, src/core/variables/resolve.test.ts, e2e/variables-binding.spec.ts |
| Modes: add, duplicate, set default, reorder; apply modes to frames, sections, pages; Auto inheritance | variables/modes-for-variables.html | P0 | Resolver | M8 | Implemented (modes are added with the default mode's values, duplicated next to the original, renamed, reordered, set as default (the first column) and deleted (layers and pages set to a deleted mode go back to Auto); modes are set on layers, frames, groups, sections and pages, and a layer on Auto uses the nearest mode set on its containers or page, else the collection's default mode; Apply variable mode in the Appearance section (layers) and the Page section sets Auto (showing the default mode) or a mode per collection with more than one mode; a layer with a variable mode set shows a tag in the layers panel with the mode's name, or the number of modes (listed on hover)) | src/editor/commands/variables.test.ts, src/core/variables/resolve.test.ts, e2e/variables-binding.spec.ts |
| Apply variables to all bindable properties (the `=` shortcut in number fields), detach | variables/apply-variables-to-designs.html | P0 | Property registry | M8 | Implemented (variables bind to opacity, visibility, width and height and their limits, corner radius, stroke weight, gap and padding, text content, font family, style and size, line height, letter spacing, and paragraph spacing and indent, and color variables to solid fills and strokes; bound properties update when values, aliases or modes change (instances included); changing a bound property directly detaches it; variables are offered only for properties in their scopes; in the right sidebar, = in the width, height, min and max size, opacity, corner radius, stroke weight, gap, padding (per side) and font size fields opens Apply variable (the variables in scope, with their values on the layer), a bound field shows the variable's name (click to change it, Delete or Detach variable to detach it), and a fill or stroke bound to a color variable shows the variable with Detach variable; color variables are applied from the fill and stroke pickers (square swatches, below the color styles), replacing the fills or strokes with one bound solid paint; a Text section for text layers edits their text content and applies a string or number variable to it, the Appearance section's visibility toggle applies a boolean variable when right-clicked, the Typography section applies variables to the font family and style, and = in line height, letter spacing, paragraph spacing and paragraph indent opens Apply variable; each bound property shows its variable with Detach variable) | src/editor/commands/variables.test.ts, src/core/variables/resolve.test.ts, e2e/variables-binding.spec.ts |
| Scopes, code syntax (Web/Android/iOS), hide from publishing | variables/create-and-manage-variables-and-collections.html | P1 | Variable store | M8 | Implemented (Edit variable in the variables view sets scopes (Show in all supported properties, or per property for color, number and string variables) and a code syntax name for Web, Android and iOS; variables are offered in the right sidebar only for properties in their scopes; hide from publishing keeps a variable out of what the file lends: importing a library brings across the variables its components are bound to, in collections of their own, and remaps the bindings to this file's copies, while a variable marked hidden stays behind and the component simply loses that binding) | src/editor/commands/variables.test.ts |
| DTCG JSON import / export of modes | variables/modes-for-variables.html | P1 | Serialization | M8 | Implemented (a mode exports as DTCG JSON: groups from slash-separated names, sRGB colors with hex, numbers, strings, booleans marked `com.openframe.type`, `{group.name}` references and `com.openframe.aliasData` for aliases to other collections; importing reads color (sRGB, HSL), dimension (px), font family, duration (s), number (or boolean) and string tokens, normalizes nested names with slashes, keeps the first of duplicate names and skips unsupported tokens, into a new mode (creating missing variables) or an existing mode (updating matching variables), with references as aliases; Import mode and Export mode on mode columns, and Import modes and Export modes on collections, in the variables view) | src/core/variables/resolve.test.ts, src/editor/commands/variables.test.ts |
| Extended collections (inherit variables, override values) | variables/extend-a-variable-collection.html | P2 | Variable store | M8 | Implemented (Extend collection in a collection's context menu creates a collection that uses its parent's variables and modes (kept in step as modes are added, renamed, reordered and deleted; variables and modes can't be added, renamed or deleted from the extension); values edited in the extension override the parent's in that mode and are highlighted, with Reset change (an override equal to the inherited value isn't kept; a color's value and opacity are one override); a mode set on a layer or page for the extended collection resolves the parent's variables with the overrides, nearest extension first; Import mode into an extension's mode overrides matching variables, and Export mode exports the overridden values; deleting a parent collection deletes its extensions) | src/editor/commands/variables-extended.test.ts, src/core/variables/resolve.test.ts, e2e/variables-extended.spec.ts |
| Variables bound to variant properties | variables/modes-for-variables.html | P1 | Components | M8 | Implemented (Assign variable next to an instance's variant property binds a string or number variable, or a boolean for true/false properties; the instance switches to the variant whose value matches the variable's value in the modes it uses, and follows value and mode changes, keeping its variables and modes; picking another variant by hand detaches the variable; Detach variable keeps the current variant) | src/editor/commands/variables-extended.test.ts |
| Libraries: publish, subscribe, review updates, swap libraries | create-and-share-libraries/*.html, use-libraries/*.html, manage-your-libraries/*.html | P1 | Local package import | M14 | Implemented as a local equivalent (publishing and subscribing are a cloud service, so offline: Import library in the Assets panel reads another `.openframe` file and copies the components it lends onto a page of its own, marked as that library and named after the file. Everything brought in takes ids of this file, and the references a component keeps to its own layers are remapped as they are copied. Check for updates reads a newer copy and says what it adds, redraws or no longer has, matching by name: a redrawn component is rebuilt in place so its instances keep pointing at it and take the new design, a new one is brought in, and one that is gone is left where it is rather than pulled out from under the instances using it. Swap for… points the instances of one library's components at another library's of the same name, and Remove takes a library out with its components) | src/editor/commands/libraries.test.ts, e2e/libraries.spec.ts |
| Check designs (lint against library) | use-libraries/check-designs-in-reference.html | P2 | Styles, Variables | M14 | Implemented (Check designs reads the page against what the file has to build from: a fill, stroke, corner radius, gap or padding written out by hand where a variable already carries that very value, text carrying no text style, and a frame or group drawing what a library component draws without being an instance of it. Each is listed with the layer, which clicking selects and brings into view; where a variable would do, Use binds it, which puts the issue right and takes it off the list. A value no variable carries is not reported, since there is nothing to put in its place) | src/editor/commands/check-designs.test.ts, e2e/libraries.spec.ts |
| UI kits (Material, Apple, Simple Design System) | use-libraries/start-designing-with-ui-kits.html | P3 | Libraries | — | Not feasible offline (third-party licensed assets; users can import their own) | |

## 10. Prototyping

| Feature | Documentation | Pri | Dependency | M | Status | Tests |
|---|---|---:|---|---|---|---|
| Prototype tab, connections (drag + noodle), interaction details panel, bulk editing | create-prototypes/connect-your-prototype.html | P0 | Prototype graph | M10 | Implemented (the right sidebar's Prototype tab lists the selected layers' interactions; + adds one — to each selected layer in bulk — with the first trigger the layer has room for, navigating instantly; an interaction opens its details: trigger, actions (Add action for several), destination (other top-level frames on the page, or layers in the same frame for Scroll to), animation and state management; interactions are undoable and saved with the file. connections show as noodles on the canvas (see View prototype connections); with the Prototype tab open, a + on the selection's right edge drags out a noodle, and dropping it on another top-level frame adds an interaction navigating there to every selected layer (the frame under the pointer is outlined), Select matching interactions in an interaction's details selects the identical interactions (the same actions and destinations) on the matching layers in the other top-level frames, and while they are selected together the Prototype tab shows one Interaction details that changes them all; when there are matching interactions on the canvas, only the first connection — the top-left one in view — is drawn, and selecting it draws the matching connections in view as well, which can then be clicked or marquee-selected) | src/core/prototype/reactions.test.ts, src/editor/commands/prototype.test.ts, src/editor/commands/matching-interactions.test.ts, e2e/prototype-interactions.spec.ts, e2e/prototype-connections.spec.ts, e2e/prototype-connect-drag.spec.ts, e2e/prototype-matching-interactions.spec.ts, src/editor/chrome/shown-connections.test.ts, e2e/prototype-matching-connections.spec.ts |
| Triggers: click/tap, drag, while hovering, while pressing, key/gamepad, mouse enter/leave/down/up, after delay, video hits/ends | guides/prototype-triggers.html | P0 | Runtime | M10 | Implemented (interactions take On click, On drag, While hovering, While pressing, Key/Gamepad (a key or key combination, pressed into the Key field), Mouse enter, Mouse leave, Mouse down, Mouse up and After delay (with its delay); a layer has each trigger once except keys and drags, and On click and While hovering exclude each other; the triggers run in presentation view (On drag runs its actions once the pointer moves while pressed; an On drag interaction with an animation moves back and forward through it as the pointer drags — along the way its moving frame travels, or for Dissolve and Smart animate the way the pointer first moved, as a share of the screen — and letting go past halfway finishes the transition from there, while letting go before it runs the transition back to the screen it left); on a layer with a video fill, When video hits (a time in seconds; a layer can have several) and When video ends run as its video plays in presentation view; Key/Gamepad also takes a button of a connected gamepad with the standard layout (Xbox One, PlayStation 4 and Switch Pro controllers, named Xbox / PlayStation style), pressed while the Key field has focus, and pressing it in presentation view runs the interaction) | src/core/prototype/reactions.test.ts, src/core/prototype/player-media.test.ts, src/core/prototype/gamepad.test.ts, e2e/prototype-interactions.spec.ts, e2e/presentation-video-interactions.spec.ts, e2e/presentation-gamepad.spec.ts, src/core/prototype/drag-transition.test.ts, e2e/presentation-drag-transition.spec.ts |
| Actions: navigate, back, scroll to, open link, open/close/swap overlay, set variable, set variable mode, conditional, video controls, change to | guides/prototype-actions.html | P0 | Runtime | M10 | Implemented (interactions run Navigate to, Back, Scroll to, Open link, Open overlay, Swap overlay and Close overlay, with a destination where they have one; they run in presentation view (Back returns to the previous screen, and swapping an overlay keeps its place in the history); Change to switches variants (see Interactive components); Set variable, Set variable mode and Conditional also run — see Variables in prototypes; Video plays, pauses or toggles, mutes, unmutes or toggles the sound of, jumps forward or backward by seconds in, or sets the time of a video in the layer's top-level frame) | src/core/prototype/reactions.test.ts, src/core/prototype/player-media.test.ts, e2e/prototype-interactions.spec.ts, e2e/presentation-video-interactions.spec.ts |
| Multiple actions, conditionals, expressions | advanced-prototyping/multiple-actions-and-conditionals.html, use-expressions-in-prototypes.html | P1 | Expression engine | M10 | Implemented (an interaction's actions run in order, so a Conditional sees values set before it; the Conditional action has an If condition with its actions and Else actions; the Set variable value and the If condition are expressions — numbers, strings in quotes, true and false, variables as {name} or {name:mode}, + - * /, == != > < >= <=, and, or, not — outlined in red while invalid; actions run top to bottom and are reordered by dragging their handle onto another action (going before it) or onto an Add action, Add if action or Add else action button (going to the end of that list, which moves actions into and out of Conditionals), or with ↑ and ↓ on the handle) | src/core/prototype/expressions.test.ts, src/core/prototype/action-paths.test.ts, src/editor/prototype-variables.test.ts, e2e/prototype-variables.spec.ts, e2e/prototype-action-order.spec.ts |
| Transitions: instant, dissolve, smart animate, move in/out, push, slide in/out, with directions | guides/prototype-animations.html | P0 | Animation core | M10 | Implemented (an action's animation is Instant, Dissolve, Smart animate, Move in, Move out, Push, Slide in or Slide out, with a direction (left, right, up, down) and Animate matching layers for the moving ones, and easing and duration (1–10,000 ms); Scroll to is instant or animated; presentation view plays them with their easing and duration; Smart animate animates matching layers (see Smart animate matching); with Animate matching layers on a moving transition, the frames move without their matching layers, which smart animate in place above them on a frame without fill) | src/core/prototype/reactions.test.ts, src/core/prototype/presentation.test.ts, e2e/prototype-interactions.spec.ts |
| Easing presets, custom bezier, springs (gentle/quick/bouncy/slow/custom) | guides/prototype-easing-and-spring-animations.html | P0 | Animation core | M10 | Implemented (an animation's easing is Linear, Ease in, Ease out, Ease in and out, Ease in back, Ease out back, Ease in and out back or a custom bezier (X1, Y1, X2, Y2), or the Gentle, Quick, Bouncy or Slow spring or a custom spring (stiffness, damping, mass), which sets its own duration; the curve and spring solvers are shared with Motion; below the easing, a graph plots it — time across, the animation up, widening when a curve or spring overshoots — and a custom bezier's two handles are dragged on it (or moved with the arrow keys) and clamped to 0–1 across, with clicking a keyframe putting its handle back; a custom spring is dragged on its graph, right for more stiffness and up for less damping, while mass stays in its field; hovering the preview beside the graph plays a dot along the easing over the animation's duration, repeating) | src/core/anim/easing.test.ts, src/core/prototype/reactions.test.ts, e2e/prototype-interactions.spec.ts, src/core/anim/easing-curve.test.ts, e2e/prototype-easing-graph.spec.ts |
| Smart animate matching (name + hierarchy) | advanced-prototyping/smart-animate-layers-between-frames.html | P1 | Animation core | M10 | Implemented (in presentation view, Smart animate matches layers between the frame left and the destination by name and place in the hierarchy, and animates the differences in position, rotation (the shorter way round), scale, size, opacity, corner radius and solid fill colors with the interaction's easing and duration; layers only in the destination dissolve in and layers only in the frame left dissolve out; frames whose matching layers have drop or inner shadows, or change from one shape to another, dissolve instead; swapping overlays smart animates between them, opening one doesn't. Animate matching layers on Move in/out, Push and Slide in/out moves the frames without their matching layers while those layers smart animate in place (layers only in the destination under a matching parent dissolve in with them); with Animate matching layers, matching fixed layers get no transition, and fixed layers directly in either frame without a match dissolve where they are instead of moving with their frames; fills blend paint by paint when a layer has as many in both frames — gradients of the same kind, or a gradient and a solid, blend their colors at every stop of either along with their placement and opacity, and image fills or gradients of different kinds cross-fade, the new paint over the old) | src/core/prototype/smart-animate.test.ts, src/core/prototype/fill-blend.test.ts, src/core/prototype/fixed-matching.test.ts, src/core/prototype/presentation.test.ts, e2e/presentation.spec.ts |
| Overlays: position, close on click outside, background | create-prototypes/create-overlays-in-your-prototypes.html | P0 | Runtime | M10 | Implemented (a frame an interaction opens as an overlay (or swaps in) shows an Overlay section in the Prototype tab: position (center, top left/center/right, bottom left/center/right), Close when clicking outside, and Add background behind overlay with its color and opacity; the settings belong to the overlay frame and are saved with the file; the player keeps open overlays stacked above the screen. Manual places the overlay where each interaction opening it says: its Overlay X and Y, set in the interaction's details, from the top-left of the layer the interaction is on (on the screen or in another overlay). In the Prototype tab each overlay frame shows a blue badge just outside its top-right corner: clicking it selects the overlay (showing its settings), and Delete then deletes the overlay, removing the Open overlay and Swap overlay actions that lead to it while the frame stays) | src/core/prototype/flows.test.ts, src/core/prototype/player.test.ts, src/core/prototype/overlay-position.test.ts, src/editor/commands/prototype-remove.test.ts, e2e/prototype-flows.spec.ts, e2e/prototype-overlay-manual.spec.ts, e2e/prototype-overlay-badge.spec.ts |
| Scroll overflow (horizontal / vertical / both), fixed and sticky layers | create-prototypes/prototype-scroll-and-overflow-behavior.html | P0 | Runtime | M10 | Implemented (the Prototype tab's Scroll behavior section sets a frame's Overflow (No scrolling, Horizontal, Vertical, Both directions), warning when the content isn't bigger than the frame, and a layer's Position in a scrolling frame (Scroll with parent, Fixed — not in auto layout unless absolutely positioned — or Sticky, which needs vertical scrolling); in presentation view the wheel or trackpad scrolls the deepest frame under the pointer with room to move that way (nested frames included), fixed layers stay put above the scrolling content, sticky layers stop at the frame's top, hotspots move with the content, and Scroll to scrolls a layer's scrolling frame to it (animated with the interaction's easing). Dragging a finger on a touch screen scrolls the same way; a sticky layer nested in another layer sticks at the frame's top but stays within its direct parent's bounds, scrolling away with the parent; fixed layers are labeled Fixed in the Layers panel) | src/core/prototype/scroll.test.ts, e2e/presentation.spec.ts, e2e/presentation-touch-scroll.spec.ts |
| State memorization and reset (scroll, component, video) | guides/state-management-for-prototypes.html, create-prototypes/preserve-scroll-position-in-prototypes.html | P1 | Runtime | M10 | Implemented (presentation view remembers each scrolling frame's scroll position when you navigate away and back, and an interaction's Reset scroll position starts its destination's scrolling frames over; navigating between matching top-level frames (identical names, or a common prefix before a slash such as Checkout / Empty and Checkout / Complete, in the same page or section) shares scroll position: the destination's scrolling frames take the scroll of their matching layers (same name and place in the hierarchy) in the frame left, within their own limits, unless the interaction resets scroll position; an interaction's Reset video state restarts its destination's videos from the beginning, playing again only if they autoplay; interactive components remember the variants they switched to while you navigate, and an interaction's Reset component state puts its destination's back; navigating between frames that share scroll position also shares the other states: an interactive component interacted with in the frame left switches the matching instance in the destination to its variant (when that variant is in the instance's own component set), unless the interaction resets component state, and a matching layer with a different video takes the time and play state of the video left, unless the interaction resets video state — layers with the same video already play one video) | src/core/prototype/scroll.test.ts, src/core/prototype/player-media.test.ts, src/core/prototype/reset-state.test.ts, e2e/presentation.spec.ts, e2e/presentation-shared-scroll.spec.ts, e2e/interactive-components-reset.spec.ts, src/core/prototype/state-sharing.test.ts, e2e/interactive-components-sharing.spec.ts, e2e/presentation-video-sharing.spec.ts |
| Flows and starting points, flow descriptions | create-prototypes/create-and-manage-prototype-flows.html | P0 | Prototype graph | M10 | Implemented (connecting two frames that have no connections makes the first a flow starting point (Flow 1, Flow 2, …); a selected top-level frame's Flow starting point section in the Prototype tab adds or removes its starting point and edits the flow's name and description; with nothing selected, the Flows section lists the page's flows with their descriptions and Select frame; flows are undoable and saved with the file. Edit description opens the Description panel, where the description is edited as formatted text — Bold, Bulleted list, Numbered list, and Add link (with its address, or the address itself where nothing is selected) format the selected text, pasted text comes in plain — and closing it (X) saves; the section, the Flows list and presentation view's flows sidebar show descriptions formatted (bold text, bulleted and numbered lists, links opening in a new tab); on the canvas, a flow starting point's tag (a preview icon and the flow's name above its frame) opens inline preview at the flow from its icon, moves the flow — name and description included — when dragged by its name onto another top-level frame (the frame is outlined while it's over it), and removes the starting point when dragged off onto empty canvas; Preview in the Flows list opens inline preview at the flow, and Copy link beside the Flow starting point heading, or Share prototype › Copy link in presentation view's toolbar (for the flow selected there), copies presentation view's address for the flow (it opens in this browser, where the file is stored). Double-clicking a tag's name opens a field on the tag that renames the flow with Enter (Escape or clicking away leaves it as it was).) | src/core/prototype/flows.test.ts, src/core/prototype/description.test.ts, src/core/prototype/description-editing.test.ts, e2e/prototype-flows.spec.ts, e2e/prototype-flow-description.spec.ts, src/editor/commands/flow-move.test.ts, e2e/prototype-flow-tag.spec.ts, e2e/presentation-share-link.spec.ts |
| Connections from main components, sections as destinations | create-prototypes/add-prototype-connections-from-main-components.html, use-sections-in-prototyping.html | P1 | Runtime | M10 | Implemented (interactions added to a main component are inherited by its instances on sync, and play from the instances in presentation view; the inherited connections aren't drawn on the canvas unless the instance (or the layer with them) is selected, while connections changed on the instance show as its own. Frames in a section are top-level frames: they are screens in presentation view, take flow starting points and can be connection destinations. A section can be the destination of a Navigate to interaction, chosen in the interaction's destination list or by dragging a connection onto the section outside its frames; while presenting, it navigates to the frame of that section visited last, or the section's first frame (top to bottom, then left to right) when none was yet. Pending: the reference rule that a main component on another page doesn't carry its connections (instances always inherit here)) | src/core/prototype/sections.test.ts, e2e/prototype-sections.spec.ts |
| Variables in prototypes, variable modes in prototypes | advanced-prototyping/use-variables-in-prototypes.html, variable-modes-in-prototypes.html | P1 | Runtime + variables | M10 | Implemented (Set variable gives a local variable a new value from an expression (a hex code for colors), in the mode the hotspot uses — so layers with their own mode only change that mode's value; Set variable mode switches the page to another mode of a collection; in presentation view and inline preview the values and modes apply to a copy of the document, so every layer bound to those variables follows, while the file itself is unchanged, and Restart puts them back. Set variable mode also switches an extended collection, and its overriding values reach every layer bound to them. The Value and Condition fields list the variables that can go in them — for a value, those of the target variable's type — and picking one writes its reference. Library variables need accounts and a server, so they are not possible offline) | src/editor/prototype-variables.test.ts, e2e/prototype-variables.spec.ts |
| Presentation view: flows sidebar, restart (R), hints on click, scaling modes, fullscreen | view-prototypes/play-your-prototypes.html | P0 | Runtime | M10 | Implemented (Present in the right sidebar's header (⌘⌥Return) saves the file and opens presentation view in a new tab, starting at the selected layer's top-level frame or the page's first flow; it shows the frames with interactions (or every top-level frame when there are none) on the page background and plays their interactions: clicks, drags, hovering, pressing, mouse enter/leave/down/up, Key/Gamepad and After delay run their actions, with Navigate to, Back, Scroll to, Open link (in a new tab) and Open/Swap/Close overlay (at the overlay's position, with its background and closing when clicking outside) animated by their transitions; screens taller than the window scroll; the footer moves between screens (← →) and restarts the flow (R); the Flows sidebar lists the flows with their descriptions and restarts at one; Options holds Show hints on click (a click that misses every hotspot outlines them) and the scaling modes (Actual size, Responsive — the screen's frame laid out at the window's width, and its height unless the frame is taller, in a copy of the document where its layers follow their constraints and auto layout, or at the device's screen size in a device — Fit width, Fit width and height, Fill screen); Fullscreen (F). Options also has Enable keyboard shortcuts (turned off, R, ← → and F do nothing, while the prototype's own Key/Gamepad interactions still run) and Hide UI (the toolbar, footer and flows sidebar hide, the address gets hide-ui=1 so reopening it keeps them hidden, a message says how to restore them, and Show UI brings them back). Comments and sharing links (including a link that hides the UI for whoever opens it) need accounts and a server, so they are not possible offline) | src/core/prototype/player.test.ts, src/core/prototype/presentation.test.ts, src/app/present-params.test.ts, e2e/presentation.spec.ts, e2e/presentation-options.spec.ts, src/editor/prototype-runtime.test.ts, e2e/presentation-responsive.spec.ts |
| Inline preview (⇧Space), follow prototype | view-prototypes/play-your-prototypes.html | P1 | Runtime | M10 | Implemented (Preview in the right sidebar's header, or ⇧Space, opens the prototype in a window over the canvas, starting at the selected frame or the page's first flow; it plays interactions like presentation view, follows edits to the file, and jumps to the frame selected on the canvas; while it has focus, ← → move between screens and R restarts without reaching the editor's shortcuts; Restart, Options (Show hints on click, Follow prototype — the canvas selection follows the preview — Resize window to 100% (Resize device to 100% with a device: the window fits the frame, or the device around it, at its size), Respect aspect ratio without a device (the window's height follows its width in the current frame's proportions, also while it is resized) and the scaling modes), Open in presentation view and Close sit in its header; its bottom-left corner resizes it (⇧ keeps the proportions); clicking a flow starting point's preview icon on the canvas opens it at that flow; the scaling modes include Responsive, as in presentation view) | e2e/inline-preview.spec.ts, src/core/prototype/device.test.ts, e2e/prototype-flow-tag.spec.ts |
| Device and background settings, device frames (own artwork) | view-prototypes/set-prototype-device-and-background-settings.html | P1 | Presentation | M10 | Implemented (with nothing selected, the Prototype tab's Prototype settings choose the Device — None, Custom size (Fit), Presentation (Full), or a phone, tablet, desktop or watch preset; when none is chosen and the first screen has a device preset's size, that device is used, as with the reference's frame presets — its Orientation (portrait or landscape), its Model (Black, Silver, Gold or Blue: the color presentation view and inline preview draw the device's body in, saved with the page and kept when the device or orientation changes) and the Background color behind the prototype; presentation view plays the prototype inside the device, drawn with Openframe's own device body (no product artwork), the screen filling the device's width and scrolling when taller, while Custom size and Presentation fit it to the window; inline preview shows phone, tablet and watch devices only. presentation view's footer has Switch device: another device of the same kind, how it fits the window (Fit device on screen, Zoom device to fill screen, Show device at 100%, which Z steps through) and Show device frame, which drops the body and plays the prototype on the bare screen; these choices last for that session only, and the page keeps its own device) | src/core/prototype/device.test.ts, src/core/prototype/presentation.test.ts, e2e/prototype-device.spec.ts, e2e/presentation-device-switcher.spec.ts |
| View prototype connections | view-prototypes/view-prototype-connections.html | P0 | Chrome overlay | M10 | Implemented (while the Prototype tab is open, the canvas shows each connection as a blue noodle from its hotspot to its destination, arriving at the destination's facing side with an arrow — the selection's connections (those starting in the selected layers or their top-level frames), or every connection on the page with nothing selected — and each flow starting point's name in a blue tag above its frame. ⇧E shows them (switching to the Prototype tab) or hides them; clicking a noodle selects its connection — its hotspot becomes the selection and the Prototype tab opens that interaction's details — and ⇧-click adds or removes connections; dragging selected connections onto another frame changes their destination (the frame is outlined), and dropping them on empty canvas removes them. With connections selected, ⌘C copies their interaction details and ⌘X cuts them (removing the connections); ⌘V pastes them onto the selected layers, replacing an interaction whose trigger can't be combined with a pasted one. Dragging a marquee from empty canvas across noodles selects those connections, with their hotspots (⇧ adds to the selected connections). Right-clicking a connection offers Remove all interactions, which removes every interaction on the current page) | src/core/prototype/connections.test.ts, src/core/prototype/noodle-marquee.test.ts, e2e/prototype-connections.spec.ts, src/editor/commands/prototype.test.ts, src/editor/clipboard/interactions.test.ts, src/editor/commands/paste-interactions.test.ts, e2e/prototype-copy-interactions.spec.ts, e2e/prototype-connection-marquee.spec.ts, e2e/prototype-overlay-badge.spec.ts |
| Accessible prototypes (semantic HTML mirror, tab order) | view-prototypes/accessible-prototypes-in-reference.html | P1 | Presentation | M10 | Implemented (in presentation view, Tab first reaches a hidden Skip to content button; pressing it, or Options > Adapt content for screen readers, turns accessibility mode on, announced as "Now adapting content for screen readers". The frames shown are then mirrored as HTML over the canvas for screen readers and keyboard navigation: top-level frames, components and instances are sections labelled with their layer names; layers with an On click interaction are links (Navigate to, Open link) or buttons (other actions), named by the text in them or their layer name, and activating one runs the interaction; shapes with an image fill are images whose alt text is the layer name; text layers are text, keeping their paragraphs, bulleted and numbered lists (nested by indentation) and links (opening in a new tab). Content follows the layer order, or the layout order in auto layout frames. Options turns the mode off. Options > Accessibility settings opens a dialog over the prototype holding Adapt content for screen readers, which turns the mode on and off) | src/core/prototype/accessibility.test.ts, e2e/presentation-accessibility.spec.ts |
| Present offline | view-prototypes/present-prototypes-offline.html | P0 | PWA | M9 | Implemented (presentation view opens from the local file in a new tab and needs no network: the app and its rendering engine are cached for offline use, so prototypes present offline once the app has loaded; Make available offline in the options menu isn't needed and not shown) | e2e/presentation.spec.ts |
| Mobile app viewing, mirror | view-prototypes/view-prototypes-on-a-mobile-device.html | — | Cloud | — | Not feasible offline (local equivalent: open the PWA on the device, or export an HTML prototype) | |

## 11. Draw mode

| Feature | Documentation | Pri | Dependency | M | Status | Tests |
|---|---|---:|---|---|---|---|
| Draw mode UI: toolbar, streamlined sliders, large layer thumbnails | reference-draw/explore-reference-draw.html | P1 | Modes | M11 | Implemented (Draw's toolbar holds the move tools and the illustration tools — Pen and Pencil, with Brush joining them in its own row; the Layers list shows each layer as a preview of its contents, drawn by the rendering engine, in a taller row, and double-clicking a preview zooms the canvas to that layer; the properties panel adds sliders beside the fields worth feeling out — a layer's opacity, and a stroke's weight. Reaching transforms from the panel comes with the transforms row) | e2e/draw-mode.spec.ts |
| Pencil and brush tools with a secondary toolbar; ⌘-click samples a stroke | reference-draw/draw-with-illustration-tools.html | P1 | Vector model | M11 | Implemented (the Pencil (⇧P) sketches a smoothed vector path, holding Shift for a straight line, and brings its own toolbar: the color, weight and style — solid or dashed — the next sketch takes; ⌘-click (Ctrl elsewhere) on a stroke takes that stroke's color, weight and style for the sketches that follow, without drawing. The Brush (B) sketches the same way and gives its strokes a dynamic stroke, whose Frequency, Wiggle and Smoothen its toolbar sets; ⌘-click on a brush stroke takes those too) | e2e/sketch-toolbar.spec.ts, e2e/pen-pencil.spec.ts |
| Custom stretch and scatter brushes | reference-draw/draw-with-illustration-tools.html | P2 | Stroke geometry | M11 | Implemented (Create brush on a single closed vector layer's menu makes a Stretch brush, which lays the shape over the whole stroke so it bends along it, or a Scatter brush, which repeats the shape along the stroke, each copy turned to face the way it goes; both scale so the shape's height matches the stroke's weight. Brushes are kept in the file beside its styles, the Stroke section applies one to the selected layers, and the stroke is then painted as the brush's filled shape rather than a line. Publishing a brush to a library needs accounts and a server, so it is not possible offline) | src/core/vector/brush.test.ts, e2e/custom-brushes.spec.ts |
| Transforms: radial and linear repeat, apply transforms | reference-draw/create-patterns-with-transforms.html | P2 | Transform group node | M11 | Implemented (Draw mode's Transform section adds a radial or linear repeat to the selection: the layers go into a group that repeats them — around the middle of the group over an angle, or along its x or y by a spacing — without layers for the copies, which are drawn. The settings change how many copies there are and how they are spread, and Apply transforms to selection turns them into real layers and drops the transform. The group's own box covers the original rather than the copies until the transform is applied) | src/core/geometry/repeat.test.ts, src/editor/commands/transforms.test.ts, e2e/transforms.spec.ts |
| Vector edit tools, variable width, shape builder, pattern fills, noise/texture/progressive blur | (see sections 4 and 6) | P1 | — | M6 / M3 | Planned | |

## 12. Motion mode

| Feature | Documentation | Pri | Dependency | M | Status | Tests |
|---|---|---:|---|---|---|---|
| Timeline panel: play (Space), auto-keyframe, current time, duration (default 2000 ms), ms/s, loop/once/ping-pong, collapse | reference-motion/tour-the-interface/use-the-reference-motion-timeline.html | P1 | Animation core | M12 | Implemented (Motion in the toolbar's switcher opens the timeline across the bottom: Play (Space, which holds the Hand tool in every other mode), Auto-keyframe, the current time, the duration — 2000 ms for a new animation — the time unit (ms or s), the playback mode, cycling between Loop, Once and Ping-pong, and Collapse layers, which puts a layer's tracks on one row. The animation is kept on the page, so it comes back with the file) | src/core/motion/animation.test.ts, src/editor/commands/motion.test.ts, e2e/motion-timeline.spec.ts |
| Ruler, playhead, zoom; layer tracks (selected/component colors); scale and move tracks | reference-motion/tour-the-interface/use-the-reference-motion-timeline.html | P1 | Timeline UI | M12 | Implemented (the ruler marks the animation and holds the playhead; clicking it seeks. The slider at the end of the controls zooms the timeline in to 50×, as does ⌘ or Ctrl with the wheel over the ruler, which zooms about the pointer; the ruler, the keyframes and the playhead all follow the window shown. Each animated layer has a track, named and selectable, wearing the icon it has in the layers panel and, for a component or an instance, that panel's purple. Below the name a bar spans the layer's keyframes: dragging it moves the whole animation later or earlier, and pulling either end stretches it, taking the bends in the motion path along) | e2e/motion-timeline.spec.ts |
| Keyframes: add, select, move (⇧ snap), delete, diamond buttons in the inspector | reference-motion/animate-your-designs/add-select-and-delete-keyframes.html | P1 | Property registry | M12 | Implemented (in Motion a diamond sits beside every property that can be animated — X, Y, W, H, rotation, opacity and the two path trims — adding a keyframe at the playhead for what the layer is now, and taking it away where there is one. Once a property is animated, typing a value at another moment records the next keyframe there, and the canvas shows the animation at the playhead while the file keeps its own values. On the timeline a keyframe is picked out by clicking it (⇧ adds to the selection), by sweeping a box over the tracks, or by the property row's name, which picks out the whole row; the layer's name picks out everything it animates. Double-clicking a keyframe takes the playhead to it. Dragging moves keyframes in time, ⇧ snapping them to the playhead or to another keyframe, and Delete takes the selection away. Auto-keyframe records a keyframe for a property that is not animated yet when its field is changed, and shows a red bar and playhead while it is on) | src/core/motion/animation.test.ts, e2e/motion-timeline.spec.ts |
| Easing: presets, hold, custom bezier, springs, saving as variables | reference-motion/animate-your-designs/adjust-an-animation-s-easing.html | P1 | Animation core | M12 | Implemented (the stretch between two keyframes carries the easing that shapes it, chosen from the menu on that stretch in the timeline: the seven Bézier presets, the four spring presets, Hold, and a custom Bézier or spring. Picking a keyframe opens an Easing section in Motion's sidebar with the same menu, the custom curve's four handles or the spring's stiffness, damping and mass, and the graph the curve is dragged on and previewed in. Save as variable keeps the easing as a variable of the new Easing type, which the stretch then follows by name and which Detach turns back into a plain curve; an easing standing as a variable is looked up when the animation is read, and one whose variable is gone runs straight) | src/animation/easing.test.ts |
| Preset animation styles (e.g. spin), composite styles | reference-motion/animate-your-designs/quickly-add-motion-with-preset-animation-styles.html | P2 | Timeline | M12 | Implemented (Motion's Animations section adds a preset animation to the selected layers, keyframed from the playhead over half a second and worked out from what each layer is now: Fade in and out, Slide in and out, Scale up and down, and Spin, which turns a layer a full circle. The composite styles — Pop in, which fades and springs into place, and Fly in, which fades while sliding — write several at once. The section lists the properties a layer is animated on, each of which can be taken off again) | src/editor/commands/motion.test.ts, e2e/motion-timeline.spec.ts | |
| Motion path editing | reference-motion/animate-your-designs/edit-an-object-s-motion-path.html | P2 | Vector model | M12 | Implemented (a layer selected in Motion with position keyframes shows its motion path on the canvas: a box marking where it is at each keyframe, dots along the stretches between them that bunch up where easing slows the layer down, and a round handle in the middle of each stretch. Dragging a box takes that keyframe anywhere on the canvas, writing the layer's x and y at that moment, so a layer keyframed on one axis gains the other. Dragging the handle bends that stretch into a curve the layer then travels; easing shapes its pace along the curve rather than the curve itself. The reference reveals the bend handle only while ⌘ is held, whereas here it always sits on the path) | src/editor/commands/motion.test.ts, e2e/motion-timeline.spec.ts |
| Path trim animation | reference-motion/animate-your-designs/animate-strokes-with-path-trim.html | P2 | makeTrimmed | M12 | Implemented (a stroke is drawn along only the share of its path between Trim and End in the Stroke section, so it draws itself on or erases itself away; a start past the end wraps back around the path, which is how a spinner is made. Both take keyframes in Motion like any other property, and the Path preset animation draws the stroke on from where the trim starts. As in the reference the path must be stroked down its middle, so the fields appear on a centered stroke alone and the Path preset is disabled without one) | src/engine/render/stroke-render.test.ts, e2e/stroke.spec.ts |
| Anchor point (⌥R) | reference-motion/animate-your-designs/move-a-layer-s-anchor-point.html | P2 | Transforms | M12 | Implemented (a layer turns and scales around its anchor point, the middle of it until one is set. In Motion, Edit anchor point in the Transform section or ⌥R reveals the target on the canvas, which is dragged to anywhere on the layer, and Reset anchor point puts it back in the middle. A layer whose size animates keeps its anchor still, so it scales around it) | src/editor/commands/motion.test.ts, e2e/motion-timeline.spec.ts | |
| Animated export: MP4, WebM, GIF, SVG (fps, size, quality, loop) | import-and-export/export-animations-from-reference.html | P1 | Export worker | M12 | Browser limitation (MP4/WebM need WebCodecs; GIF and SVG always available) | |
| Time-stamped comments | comments/add-comments-to-files.html | P3 | Comments | M14 | Implemented (a comment left while Motion is in hand is about a moment as well as a place: the playhead is written down with it, the list says which moment it is about, and opening it takes the playhead back there along with the canvas) | e2e/comments.spec.ts |

## 13. Dev Mode

| Feature | Documentation | Pri | Dependency | M | Status | Tests |
|---|---|---:|---|---|---|---|
| Dev Mode toggle (⇧D), left sidebar (ready for dev, pages with badges, layers), frame pager | dev-mode/tour-the-interface/navigate-designs-in-dev-mode.html | P1 | Modes | M13 | Implemented (⇧D and the toolbar's switcher open Dev Mode, which stands the drawing tools down and shows the inspect panel in place of the properties panel. The left sidebar keeps the pages, layers and search (⌘F) Design uses, and adds Ready for development: the designs on the page marked for handoff, each with its status, which clicking selects and brings into view. A page holding any of them wears the Dev Mode badge) | e2e/dev-mode.spec.ts |
| Inspect panel: header, status, box model, List/Code, layout/style code blocks, colors, typography, styles and variables, interactions | dev-mode/inspect-designs/guide-to-inspecting.html | P1 | Codegen | M13 | Implemented (the inspect panel names the selected layer, shows its handoff status and the buttons that set it, and reads the design as either a List or Code. The list gives its position and rotation, its size and corner radius, an auto-layout frame's direction, padding and gap, and its opacity and blend mode, every value copied by clicking it; Code writes the layer out in the language and unit chosen, with a Copy code button) | src/core/dev/code-gen.test.ts, e2e/dev-mode.spec.ts | e2e/dev-mode.spec.ts |
| Code generation: CSS (px/rem), SwiftUI, UIKit (px/pt), Compose, Android XML (px/dp/sp); unit scale | dev-mode/turn-designs-to-code/use-code-snippets-in-dev-mode.html | P1 | Codegen | M13 | Implemented (the Code view writes the selection out as CSS, SwiftUI, UIKit, Compose or Android XML: its box, corner radius, fill, stroke and opacity, an auto-layout frame's direction, gap and padding, and a text layer's family, size, weight, line height, letter spacing, alignment and colour, the weight read from the style name. Each language offers its own units — px and rem for CSS, pt and px for iOS, dp, sp and px for Android — over a unit scale that sets the root font size or the scale factor. Code Connect, which swaps in a design system's own snippets, needs a server) | src/core/dev/code-gen.test.ts, e2e/dev-mode.spec.ts |
| Redlines on hover and ⌥-hover; saved measurements (⇧M) | dev-mode/inspect-designs/add-measurements-and-annotate-designs.html | P1 | Chrome overlay | M13 | Implemented (⌥ held over a layer measures its distance to the selection, which is the passing measuring any file can do. ⇧M takes the measurement tool, and dragging from one layer to another saves the distance on the page for everyone who opens the file: it follows the layers as they move, reads its distance or text of its own, and is renamed or deleted from the inspect panel's Measurements section. The reference edits a saved measurement's text by double-clicking it on the canvas, and drags it clear of the design; here it is edited in the panel and drawn between the layers) | src/editor/commands/measurements.test.ts, e2e/dev-mode.spec.ts |
| Annotations (⇧T), categories, live properties, filter | dev-mode/inspect-designs/add-measurements-and-annotate-designs.html | P1 | Document model | M13 | Implemented (⇧T leaves a note on the selected layer, written in the Annotations section in Dev Mode and while designing alike. A note holds free text, the category it is filed under — Development, Interaction, Accessibility and Content to begin with, which the file may then change — and the layer's own properties called out by name, so they read from the design and stay right as it changes. The filter shows one category at a time. The reference also draws its annotations on the canvas beside the layer; here they are read in the panel) | src/editor/commands/annotations.test.ts, e2e/dev-mode.spec.ts |
| Statuses: ready for dev, completed, changed (automatic) | dev-mode/dev-mode-across-your-organization/dev-mode-statuses-and-notifications.html | P1 | History | M13 | Implemented (a section or frame is marked Ready for dev or Completed from the Status section, in Dev Mode and while designing alike, and the status comes off again. Editing the design afterwards puts it in the changed state on its own — a change anywhere inside it counts, while setting the status itself, or binding a variable or a style, does not — and marking it again settles it. The reference also leaves a design unchanged when an instance is updated from a shared library, which needs the library updates of M14) | src/editor/commands/dev-status.test.ts, e2e/dev-mode.spec.ts |
| Ready-for-dev view, focus view | dev-mode/dev-mode-across-your-organization/dev-mode-ready-for-dev-view.html, dev-mode-focus-view.html | P2 | Versions | M13 | Implemented (Dev Mode's left sidebar holds the ready for dev view: every design on the page carrying a status, with the changed ones called out. Picking one opens the focus view, which gives the sidebar up to that design alone — its name, its status, and Mark as completed — with Back returning to the list; the canvas comes to the design as it opens. The reference's focus view also keeps a version history of its own for the design and lets a developer resize it or switch its variable modes for a moment; those are pending) | e2e/dev-mode.spec.ts | e2e/dev-mode.spec.ts |
| Compare changes (side by side, overlay, property and code diff) | dev-mode/inspect-designs/compare-changes-in-dev-mode.html | P2 | Versions, Diff | M13 | Implemented (Dev Mode's Compare changes reads a saved version against the file as it stands. Properties lists the layers added and taken away, and for the ones in both, every property that reads differently — name, place, size, opacity, visibility, fill, stroke, corner radius, text and an auto-layout frame's direction, gap and padding — shown then and now, each layer selectable. Code puts the selected layer's CSS either side, and Visual draws the layer both ways, beside each other or laid over one another) | src/core/dev/compare.test.ts, e2e/dev-mode.spec.ts |
| Variables in Dev Mode (details, suggested variables, variables table) | dev-mode/inspect-designs/variables-in-dev-mode.html | P1 | Variables | M13 | Implemented (the inspect panel's Variables section names the variables the selected layer's properties are bound to, its fills and strokes among them, read by their code syntax where they have one. Under them it suggests the variables that already carry what the layer holds outright — a fill or stroke of the same colour, a width, height or corner radius of the same number — so a value can be named rather than repeated. The file's whole variables table is the Variables view, which Dev Mode opens as Design does) | src/editor/commands/dev-resources.test.ts, e2e/dev-mode.spec.ts | e2e/dev-mode.spec.ts |
| Assets section (auto icon detection, downloads), export | dev-mode/inspect-designs/guide-to-inspecting.html | P1 | Export | M13 | Implemented (Dev Mode's Assets section lists what the page has to hand over: every layer set up for export by hand, offered as it was set up, and the icons found by their shape — small, near enough square, and made of drawing rather than text — offered as SVG for a drawing and PNG for the rest. Each downloads on its own or all together, and picking one selects the layer it stands for. The export settings themselves are edited in Design's Export section) | src/editor/commands/dev-assets.test.ts, e2e/dev-mode.spec.ts |
| Dev resources (links on layers) | dev-mode/turn-designs-to-code/link-dev-resources-to-layers-in-dev-mode.html | P2 | Document model | M13 | Implemented (a link is pasted onto the selected layer in the inspect panel's Dev resources section, pointing at the source, a ticket or a page of documentation, and is followed or deleted from there. An address without a scheme is taken to be a web one, and anything a browser must not follow is refused. A link on a component is inherited by its instances and shown as the component's; one added to an instance belongs to that instance alone. The links are kept in the file, and opening one is the reader's own doing) | src/editor/commands/dev-resources.test.ts, e2e/dev-mode.spec.ts |
| Component playground | dev-mode/inspect-designs/guide-to-inspecting.html | P2 | Components | M13 | Implemented (an instance selected in Dev Mode carries a Playground: its component's properties turned other ways to see what the component does — a checkbox for a boolean, a text box for text, the file's components for an instance swap — with Reset putting it back. The whole of it lives in a transaction that is never committed, so none of it reaches the file, leaves an undo step, or outlives the layer being selected; any real edit takes the file back first) | src/editor/dev/playground.test.ts, e2e/dev-mode.spec.ts |
| Animation handoff code (CSS / React / JSON) | dev-mode/turn-designs-to-code/hand-off-animations-to-development.html | P2 | Motion | M13 | Implemented (an animated layer selected in Dev Mode carries a Motion section: the code that rebuilds its animation as CSS keyframes with the animation shorthand, as a Framer Motion component with its values as arrays against where they fall in the run, or as the animation's own data in JSON, with a Copy button. Show in timeline view opens the timeline under the canvas to watch it in, where the keyframes and the timing are read but not changed. The MCP server the reference hands the same detail to needs a server) | src/core/dev/animation-code.test.ts, e2e/dev-mode.spec.ts |
| Code Connect, MCP server, VS Code extension, Dev Mode plugins, notifications, org admin settings | dev-mode/turn-designs-to-code/code-connect.html, reference-for-vs-code.html, dev-mode-across-your-organization/manage-dev-mode-settings-for-an-organization.html | — | Cloud | — | Not feasible offline | |

## 14. Import / export / files

| Feature | Documentation | Pri | Dependency | M | Status | Tests |
|---|---|---:|---|---|---|---|
| Native `.openframe` format (ZIP of canonical JSON plus blobs), versioning, migrations, validation | — (project requirement) | P0 | Serialization | M1 / M9 | Implemented (canonical JSON, validation and a migrations framework; Openframe files (.openframe) are ZIP archives of a manifest, the document JSON and the images it uses by content hash; opening one reads only those entries (each at most 512 MB), validates the manifest and document, and checks every image against its hash, refusing other or damaged files with the reason) | src/core/document/document.test.ts, src/platform/package.test.ts, src/core/document/image-references.test.ts |
| Autosave to IndexedDB, crash recovery journal, status indicator | — (project requirement) | P0 | Persistence | M1 | Implemented (committed edits survive reload and tab close; uncommitted gestures are discarded) | src/platform/idb/persistence.test.ts, e2e/foundation.spec.ts |
| Large documents (10,000 nodes: spatial index, culling, hit testing) | — (project requirement) | P0 | Scene index, renderer | M1 | In progress (CPU-surface smoke budgets met; browser frame-rate profiling and tile caching in M2) | src/perf/scene-perf.test.ts |
| Open / Save / Save As | — (project requirement) | P0 | File System Access API | M9 | Implemented (files save locally as you work; File > Save (⌘S) saves the file as .openframe to a file on disk — asking where the first time, then writing back to the same file — and Save as… asks again; File > Open file… opens an .openframe file from disk as a new local file that Save then writes back to (dropping an .openframe file on the canvas opens it too); Save local copy… downloads the file. Browsers without the File System Access API (Firefox, Safari) download a copy on Save and open files from the file picker, without saving back) | src/platform/disk-file.test.ts, e2e/save-to-disk.spec.ts, e2e/local-files.spec.ts |
| Export PNG / JPG / SVG / PDF with scale, suffix, per-layer settings, bulk export (⇧⌘E) | import-and-export/export-static-designs-from-reference.html, export-formats-and-settings-for-static-designs.html | P0 | Export worker | M9 | Implemented (layers keep export configurations in the Export section of the right sidebar: format (PNG, JPG, WebP), scale as a multiplier or a fixed width or height (typed or from presets such as 0.5x, 2x, 512w, 512h) and a file name suffix; Export saves a single layer's files (several as a ZIP archive, in folders from slash-separated layer names), Preview shows a single layer's first export, and File > Export (⇧⌘E) lists every export on the page with its size, to uncheck and save; images cover the layer's painted bounds (effects included) on a transparent background, JPG on white, and slices export everything within their bounds; SVG exports at 1x as shapes, frames (clipping their content), vectors, groups and boolean groups with solid, linear and radial gradient and image paints (the file carried in the markup as a data URI), strokes as filled outlines, per-side stroke weights as the ring they cover, text as the outlines of its glyphs, masks as SVG masks, a drop shadow and a layer blur as a filter, and opacity and blend modes; each export lists what it left out (pattern fills, angular and diamond gradients, inner shadows, background blurs, noise, and a second drop shadow, which `feDropShadow` would shadow rather than the layer)); PDF exports a one-page document the layer's own size in points, carrying the layer drawn as a JPEG — the rendering engine has no PDF of its own, so it is the picture rather than the shapes, and text in it is not selectable. Each configuration also carries the settings its format has of its own, opened under it: ignore overlapping layers (off draws the whole page and cuts it to the layer's bounds), the SVG "id" attribute and Simplify stroke (a plain centred stroke stays a stroke rather than being outlined), the color profile (the file's own, sRGB or Display P3), image quality for JPG and PDF (Low, Medium, High, Maximum — PDFs start at Medium as the reference's do), and image resampling (Detailed, which weighs the pixels around each one, or Basic, which takes the nearest). The reference's "Include bounding box" is not offered: a layer already exports as its own box, which is what that setting asks for, and SVG text is always outlined, since that is how it is exported at all) | src/core/export/export-settings.test.ts, src/core/export/pdf-document.test.ts, src/core/export/svg-document.test.ts, src/editor/commands/export.test.ts, e2e/export.spec.ts |
| WebP export | — (project requirement) | P1 | CanvasKit encoder | M9 | Implemented (WebP is one of the Export section's formats, encoded by the rendering engine) | src/core/export/export-settings.test.ts, src/editor/commands/export.test.ts, e2e/export.spec.ts |
| Import SVG as editable vectors; PNG/JPEG/WebP/GIF images; drag and drop | import-and-export/guide-to-imports-in-reference-design.html | P0 | SVG parser | M9 | Implemented (dropping or pasting SVG files on the canvas creates a frame named after the file at the SVG's size, holding its paths, rectangles (with rounded corners), circles, ellipses, lines, polylines and polygons as editable vector layers and its groups as groups; the view box, transforms, solid fills and strokes (including currentColor, the even-odd fill rule and fill, stroke and layer opacity, from attributes or style declarations) carry over; like the reference, markers and patterns are not imported, and neither are text, images, reused elements, masks or gradient paints — a notice lists what was left out; PNG, JPEG, WebP and GIF images import as image layers — see Image placement) | src/core/import/svg-import.test.ts, src/core/import/svg-path-parse.test.ts, src/core/import/svg-color.test.ts, src/core/import/svg-transform.test.ts, src/editor/commands/import-svg.test.ts, e2e/svg-import.spec.ts |
| Copy assets between design tools (SVG clipboard) | import-and-export/copy-assets-between-design-tools.html | P1 | Clipboard | M9 | Implemented (SVG markup copied from another design tool pastes as editable vectors, the same as importing an SVG file; Copy as SVG copies a layer's SVG for other tools; pasting other tools' native clipboard formats isn't possible offline) | e2e/svg-import.spec.ts, e2e/copy-as.spec.ts |
| Import .fig / .sketch files | import-and-export/import-sketch-files.html | P3 | Proprietary formats | — | Not feasible (undocumented proprietary binary formats) | |
| PWA install, offline startup, update handling | — (project requirement) | P0 | Service worker | M1 / M9 | Implemented (precached shell, assets, fonts and CanvasKit, with the large color emoji font cached right after the app first starts rather than at install, and each Noto Sans CJK subset cached the first time text needs it; web manifest and icons; waiting updates applied via an in-app Reload after autosave flush; `.openframe` file handlers in M9) | e2e/offline.spec.ts |
| No-network guarantee (CSP, lint ban on network APIs, E2E network and error guards) | — (project requirement) | P0 | Tooling | M0 | Implemented | e2e/fixtures.ts |

## 15. Collaboration equivalents

| Feature | Documentation | Pri | Dependency | M | Status | Tests |
|---|---|---:|---|---|---|---|
| Comments: pins, regions, threads, resolve/unresolve, edit, delete, move, filter/sort, markdown formatting, images | comments/*.html | P1 | Sidecar store | M14 | Implemented (C takes comment mode, where the right panel gives itself over to the comments and nothing on the canvas is edited. Clicking pins a comment, dragging marks a region, and one left inside a top-level frame or group hangs from it and travels with it; dragging a pin takes the comment clear of the design. The panel searches the threads, sorts them newest, oldest or unresolved first, and opens one to reply, rewrite a message — which is then marked edited — delete a message or the whole thread, and resolve or unresolve it. A message carries **bold**, *italic*, ~~strikethrough~~, `code` and links, written as text and never as HTML, and an image picked from disk and kept in the file. Pins and region outlines are drawn on the canvas, and ⇧C keeps them off it. The author is this device; mentions, reactions and notifications need a server) | src/editor/commands/comments.test.ts, e2e/comments.spec.ts |
| Version history: auto and named versions, restore, duplicate | branching-and-merging/guide-to-branching.html | P1 | Snapshots | M9 | Implemented (Show version history, from the File menu or the dropdown next to the file name, opens version history in the right sidebar: the current version, then saved versions and autosave checkpoints, newest first; + saves the file to version history with a title and description; saves add an autosave checkpoint every 30 minutes while the file changes; selecting a version shows it read-only (edits are discarded) and Done in the toolbar returns to the file as it is now; a version's menu (or right-click) names it (Name this version, Edit version info), restores it — adding two autosave checkpoints at the same time, one saving the file as it was and one for the restored version — or duplicates it as a new local file; versions are stored in the browser with the file. Not included: pinning and copying links to versions, and version history scoped to a design in focus view (Dev Mode, M13)) | src/platform/idb/versions.test.ts, src/core/history/history-read-only.test.ts, src/app/version-history.test.ts, e2e/version-history.spec.ts |
| Branches: create, switch, update from main, 3-way merge with conflict resolution, archive | branching-and-merging/*.html | P2 | Diff/merge | M14 | Implemented (Branches makes a copy of the file to work on apart from it: the file as it stands is kept as a version, which is the common ground a merge reads from, and the copy opens as a branch. A branch says which file it came from and goes back to it, and either side merges the other in. The merge is a three-way one: a change made on one side alone is taken, a change both made the same way is already there, a layer added comes across and one deleted goes — unless it was changed here, which is left to settle. A field the two moved apart on is left as it is here and listed as a conflict, with the layer to select and the other side's value to take) | src/core/branch/merge.test.ts, e2e/branches.spec.ts |
| Multiplayer cursors, spotlight, cursor chat, viewer history, share links, email notifications | multiplayer-tools/*.html, comments/manage-email-notifications-for-comments-on-files.html | — | Server | — | Not feasible offline | |
